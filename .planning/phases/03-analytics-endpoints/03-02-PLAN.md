---
phase: 03-analytics-endpoints
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend_monitoramento/api/views/analytics.py
  - backend_monitoramento/api/views/__init__.py
  - backend_monitoramento/api/urls.py
autonomous: true
requirements: [ANA-01, ANA-02, ANA-03]

must_haves:
  truths:
    - "GET /api/analytics/potencia/ retorna media_geral_kw e por_provedor com valores corretos"
    - "GET /api/analytics/ranking-fabricantes/ retorna top 5 provedores ordenados desc por inversores ativos"
    - "GET /api/analytics/mapa/ retorna todas as usinas com latitude, longitude, provedor, status — sem paginacao"
    - "Os 3 endpoints rejeitam requisicoes sem token com 401"
    - "Nenhuma query N+1 — todas as views usam select_related ou annotate"
  artifacts:
    - path: "backend_monitoramento/api/views/analytics.py"
      provides: "PotenciaMediaView, RankingFabricantesView, MapaUsinasView"
      exports: ["PotenciaMediaView", "RankingFabricantesView", "MapaUsinasView"]
      min_lines: 50
    - path: "backend_monitoramento/api/urls.py"
      provides: "3 paths novos: analytics/potencia/, analytics/ranking-fabricantes/, analytics/mapa/"
      contains: "analytics/"
  key_links:
    - from: "backend_monitoramento/api/views/analytics.py"
      to: "usinas.models.Usina"
      via: "Usina.objects.filter().select_related('ultimo_snapshot').aggregate(Avg(...))"
      pattern: "Avg.*ultimo_snapshot__potencia_kw"
    - from: "backend_monitoramento/api/views/analytics.py"
      to: "usinas.models.Inversor"
      via: "Inversor.objects.values().annotate(Count(filter=Q(...)))"
      pattern: "Count.*filter.*pac_kw__gt"
    - from: "backend_monitoramento/api/urls.py"
      to: "backend_monitoramento/api/views/analytics.py"
      via: "path('analytics/...', View.as_view())"
      pattern: "analytics/"
---

<objective>
Implementar os 3 endpoints de analytics (potencia media, ranking fabricantes, mapa) e fazer todos os testes do Plan 01 passarem (GREEN phase do TDD).

Purpose: Entregar os dados agregados que o frontend consumira na Phase 6 (dashboard analitico).
Output: 3 endpoints funcionais e testados — PotenciaMediaView, RankingFabricantesView, MapaUsinasView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-endpoints/03-CONTEXT.md
@.planning/phases/03-analytics-endpoints/03-RESEARCH.md
@.planning/phases/03-analytics-endpoints/03-01-SUMMARY.md
@backend_monitoramento/usinas/models.py
@backend_monitoramento/api/views/__init__.py
@backend_monitoramento/api/views/usinas.py
@backend_monitoramento/api/views/garantias.py
@backend_monitoramento/api/urls.py
@backend_monitoramento/api/serializers/analytics.py
@backend_monitoramento/api/tests/test_analytics.py

<interfaces>
<!-- Contratos criados no Plan 01 que esta task implementa -->

From backend_monitoramento/api/serializers/analytics.py (criado no Plan 01):
```python
class UsinaMapaSerializer(serializers.ModelSerializer):
    status = serializers.SerializerMethodField()
    class Meta:
        model = Usina
        fields = ['id', 'nome', 'provedor', 'latitude', 'longitude', 'ativo', 'status']
    def get_status(self, obj) -> str:
        if obj.ultimo_snapshot is None:
            return 'sem_dados'
        return obj.ultimo_snapshot.status
```

From backend_monitoramento/usinas/models.py (apos Plan 01):
```python
class Usina(models.Model):
    provedor = models.CharField(max_length=30)
    ativo = models.BooleanField(default=True)
    latitude = models.FloatField(null=True, blank=True)   # Adicionado D-01
    longitude = models.FloatField(null=True, blank=True)   # Adicionado D-01
    ultimo_snapshot = models.OneToOneField('SnapshotUsina', null=True, blank=True, ...)

class Inversor(models.Model):
    usina = models.ForeignKey(Usina, related_name='inversores', ...)
    ultimo_snapshot = models.OneToOneField('SnapshotInversor', null=True, blank=True, ...)
```

From backend_monitoramento/api/urls.py (padrao atual):
```python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
# ... imports existentes
urlpatterns = [
    path('auth/token/', ...),
    path('garantias/', ...),
    path('coleta/logs/', ...),
    path('', include(router.urls)),
]
```

From backend_monitoramento/api/views/__init__.py:
```python
from .logs import LogColetaListView
class PingView(APIView): ...
```

Estrutura JSON esperada pelos testes:

ANA-01 /api/analytics/potencia/:
```json
{
  "media_geral_kw": 4.25,
  "por_provedor": [
    {"provedor": "hoymiles", "media_kw": 3.0, "usinas_ativas": 1},
    {"provedor": "solis", "media_kw": 5.5, "usinas_ativas": 1}
  ]
}
```

ANA-02 /api/analytics/ranking-fabricantes/:
```json
{
  "ranking": [
    {"provedor": "solis", "inversores_ativos": 42},
    {"provedor": "hoymiles", "inversores_ativos": 28}
  ]
}
```

ANA-03 /api/analytics/mapa/:
```json
[
  {"id": "uuid", "nome": "...", "provedor": "...", "latitude": -23.55, "longitude": -46.63, "ativo": true, "status": "normal"},
  {"id": "uuid", "nome": "...", "provedor": "...", "latitude": null, "longitude": null, "ativo": true, "status": "sem_dados"}
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar PotenciaMediaView, RankingFabricantesView e MapaUsinasView</name>
  <files>
    backend_monitoramento/api/views/analytics.py,
    backend_monitoramento/api/views/__init__.py
  </files>
  <read_first>
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/views/garantias.py,
    backend_monitoramento/api/views/__init__.py,
    backend_monitoramento/api/serializers/analytics.py,
    backend_monitoramento/api/tests/test_analytics.py,
    .planning/phases/03-analytics-endpoints/03-RESEARCH.md
  </read_first>
  <action>
Criar `backend_monitoramento/api/views/analytics.py` com 3 views:

**1. PotenciaMediaView(APIView) — ANA-01, per D-02:**
```python
from django.db.models import Avg, Count, F
from rest_framework.views import APIView
from rest_framework.response import Response
from rest_framework import generics
from usinas.models import Usina, Inversor
from api.serializers.analytics import UsinaMapaSerializer


class PotenciaMediaView(APIView):
    """
    GET /api/analytics/potencia/
    ANA-01: potencia media geral + agrupada por provedor.
    Fonte: Usina.ultimo_snapshot.potencia_kw (D-02).
    Exclui usinas sem snapshot do calculo.
    """

    def get(self, request):
        qs = (
            Usina.objects
            .filter(ativo=True, ultimo_snapshot__isnull=False)
            .select_related('ultimo_snapshot')
        )

        resultado = qs.aggregate(media_geral=Avg('ultimo_snapshot__potencia_kw'))
        media_geral = resultado['media_geral']

        por_provedor = list(
            qs
            .values('provedor')
            .annotate(
                media_kw=Avg('ultimo_snapshot__potencia_kw'),
                usinas_ativas=Count('id'),
            )
            .order_by('provedor')
        )

        return Response({
            'media_geral_kw': media_geral,
            'por_provedor': por_provedor,
        })
```

**2. RankingFabricantesView(APIView) — ANA-02, per D-03:**
```python
class RankingFabricantesView(APIView):
    """
    GET /api/analytics/ranking-fabricantes/
    ANA-02: top 5 provedores por inversores ativos.
    Criterio de ativo: ultimo_snapshot nao null E pac_kw > 0 (D-03).
    """

    def get(self, request):
        from django.db.models import Q

        ranking = list(
            Inversor.objects
            .values(provedor=F('usina__provedor'))
            .annotate(
                inversores_ativos=Count(
                    'id',
                    filter=Q(
                        ultimo_snapshot__isnull=False,
                        ultimo_snapshot__pac_kw__gt=0,
                    )
                )
            )
            .order_by('-inversores_ativos')[:5]
        )

        return Response({'ranking': ranking})
```

IMPORTANTE sobre Pitfall 3 do RESEARCH.md: usar `values(provedor=F('usina__provedor'))` para que a chave no dict seja `provedor` e nao `usina__provedor`.

**3. MapaUsinasView(generics.ListAPIView) — ANA-03:**
```python
class MapaUsinasView(generics.ListAPIView):
    """
    GET /api/analytics/mapa/
    ANA-03: todas as usinas com lat/lng, provedor e status.
    Sem paginacao — frontend precisa de todos os pontos.
    Retorna array plano (ListAPIView padrao).
    """
    serializer_class = UsinaMapaSerializer
    pagination_class = None

    def get_queryset(self):
        return (
            Usina.objects
            .select_related('ultimo_snapshot')
            .order_by('nome')
        )
```

Atualizar `backend_monitoramento/api/views/__init__.py` — adicionar import:
```python
from .analytics import PotenciaMediaView, RankingFabricantesView, MapaUsinasView
```
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && python3 -c "from api.views.analytics import PotenciaMediaView, RankingFabricantesView, MapaUsinasView; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class PotenciaMediaView" backend_monitoramento/api/views/analytics.py
    - grep -q "class RankingFabricantesView" backend_monitoramento/api/views/analytics.py
    - grep -q "class MapaUsinasView" backend_monitoramento/api/views/analytics.py
    - grep -q "Avg.*ultimo_snapshot__potencia_kw" backend_monitoramento/api/views/analytics.py
    - grep -q "Count.*filter=Q" backend_monitoramento/api/views/analytics.py
    - grep -q "pagination_class = None" backend_monitoramento/api/views/analytics.py
    - grep -q "provedor=F.*usina__provedor" backend_monitoramento/api/views/analytics.py
    - grep -q "from .analytics import" backend_monitoramento/api/views/__init__.py
  </acceptance_criteria>
  <done>
    3 views criadas em analytics.py: PotenciaMediaView (Avg por provedor), RankingFabricantesView (Count condicional top 5), MapaUsinasView (ListAPIView sem paginacao). Todas importadas via __init__.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registrar URLs e fazer todos os testes passarem</name>
  <files>
    backend_monitoramento/api/urls.py
  </files>
  <read_first>
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/views/analytics.py,
    backend_monitoramento/api/tests/test_analytics.py
  </read_first>
  <action>
1. Atualizar `backend_monitoramento/api/urls.py` — adicionar import e 3 paths:

   No bloco de imports, adicionar:
   ```python
   from .views.analytics import PotenciaMediaView, RankingFabricantesView, MapaUsinasView
   ```

   No `urlpatterns`, ANTES do `path('', include(router.urls))`, adicionar:
   ```python
   path('analytics/potencia/', PotenciaMediaView.as_view(), name='analytics-potencia'),
   path('analytics/ranking-fabricantes/', RankingFabricantesView.as_view(), name='analytics-ranking'),
   path('analytics/mapa/', MapaUsinasView.as_view(), name='analytics-mapa'),
   ```

   CRITICO: Os paths de analytics devem ficar ANTES do `include(router.urls)` para evitar que o router intercepte a URL.

2. Executar testes: `cd backend_monitoramento && python3 -m pytest api/tests/test_analytics.py -x --tb=short`
   TODOS os testes devem passar (GREEN).

3. Se algum teste falhar, investigar e corrigir a view ou o teste (ajustar valores esperados se necessario, mas manter a semantica do requisito).

4. Executar suite completa: `cd backend_monitoramento && python3 -m pytest api/tests/ --tb=short`
   Confirmar que TODOS os testes (auth + usinas + garantias + analytics) passam.
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && python3 -m pytest api/tests/test_analytics.py -x --tb=short && python3 -m pytest api/tests/ --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "analytics/potencia/" backend_monitoramento/api/urls.py
    - grep -q "analytics/ranking-fabricantes/" backend_monitoramento/api/urls.py
    - grep -q "analytics/mapa/" backend_monitoramento/api/urls.py
    - grep -q "from .views.analytics import" backend_monitoramento/api/urls.py
    - cd backend_monitoramento && python3 -m pytest api/tests/test_analytics.py -x retorna 0 (todos passam)
    - cd backend_monitoramento && python3 -m pytest api/tests/ retorna 0 (suite completa verde)
  </acceptance_criteria>
  <done>
    3 URLs de analytics registradas. Todos os testes de analytics passam. Suite completa (auth + usinas + garantias + analytics) verde. Endpoints ANA-01, ANA-02 e ANA-03 entregues.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API analytics | Dados agregados de potencia, ranking e localizacao expostos; JWT obrigatorio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | PotenciaMediaView | mitigate | IsAuthenticated global — endpoint herda automaticamente; teste test_requer_auth confirma 401 |
| T-3-02 | Information Disclosure | RankingFabricantesView | mitigate | IsAuthenticated global — mesmo mecanismo |
| T-3-03 | Information Disclosure | MapaUsinasView (lat/lng) | mitigate | IsAuthenticated global — coordenadas protegidas por JWT; apenas admin autenticado acessa |
| T-3-04 | Denial of Service | queries aggregation | accept | Volume controlado (~centenas de usinas); queries com Avg/Count sao O(n) no banco; sem risco atual |
| T-3-05 | Information Disclosure | MapaUsinasView sem paginacao | accept | pagination_class=None expoe todas as usinas de uma vez; volume < 1000 usinas; dados ja protegidos por JWT |
</threat_model>

<verification>
1. `GET /api/analytics/potencia/` com token retorna 200 e JSON com media_geral_kw e por_provedor
2. `GET /api/analytics/ranking-fabricantes/` com token retorna 200 e ranking[] com no maximo 5 itens
3. `GET /api/analytics/mapa/` com token retorna 200 e array de usinas com lat/lng/status
4. Os 3 endpoints retornam 401 sem token
5. Suite completa de testes verde: `python3 -m pytest api/tests/ --tb=short`
</verification>

<success_criteria>
- PotenciaMediaView retorna media correta usando Avg no ORM (sem N+1)
- RankingFabricantesView retorna top 5 com Count condicional (exclui sem snapshot e pac_kw=0)
- MapaUsinasView retorna todas as usinas sem paginacao, com latitude/longitude null quando ausentes
- URLs registradas em api/urls.py com prefixo analytics/
- Todos os testes de test_analytics.py passam (>= 17 testes)
- Suite completa verde (nenhum teste existente quebrado)
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-endpoints/03-02-SUMMARY.md`
</output>
