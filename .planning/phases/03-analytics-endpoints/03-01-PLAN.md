---
phase: 03-analytics-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_monitoramento/usinas/models.py
  - backend_monitoramento/api/tests/test_analytics.py
  - backend_monitoramento/api/tests/conftest.py
  - backend_monitoramento/api/serializers/analytics.py
  - backend_monitoramento/api/serializers/__init__.py
autonomous: true
requirements: [ANA-01, ANA-02, ANA-03]

must_haves:
  truths:
    - "Model Usina possui campos latitude e longitude (FloatField, null=True)"
    - "Migration reversivel existe e pode ser aplicada e revertida sem erro"
    - "Arquivo de testes test_analytics.py existe com testes para os 3 endpoints"
    - "Serializer UsinaMapaSerializer existe e exporta campos corretos"
  artifacts:
    - path: "backend_monitoramento/usinas/models.py"
      provides: "Campos latitude e longitude no model Usina"
      contains: "latitude = models.FloatField"
    - path: "backend_monitoramento/api/tests/test_analytics.py"
      provides: "Testes para ANA-01, ANA-02, ANA-03"
      min_lines: 80
    - path: "backend_monitoramento/api/serializers/analytics.py"
      provides: "UsinaMapaSerializer para endpoint de mapa"
      exports: ["UsinaMapaSerializer"]
  key_links:
    - from: "backend_monitoramento/api/tests/test_analytics.py"
      to: "backend_monitoramento/api/tests/conftest.py"
      via: "pytest fixtures (usina, tokens, snapshot_usina, inversor, snapshot_inversor)"
      pattern: "def test_.*potencia|def test_.*ranking|def test_.*mapa"
---

<objective>
Preparar a base para os 3 endpoints de analytics: migration para lat/lng no model Usina, serializer do mapa, e testes completos (RED phase do TDD).

Purpose: Estabelecer contratos (migration, serializer, testes) antes de implementar as views. Os testes definem o comportamento esperado; o Plan 02 os faz passar.
Output: Migration aplicada, serializer de mapa, arquivo de testes com todos os cenarios de ANA-01/02/03 falhando (endpoints ainda nao existem).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-endpoints/03-CONTEXT.md
@.planning/phases/03-analytics-endpoints/03-RESEARCH.md
@.planning/phases/03-analytics-endpoints/03-VALIDATION.md
@backend_monitoramento/usinas/models.py
@backend_monitoramento/api/tests/conftest.py
@backend_monitoramento/api/serializers/__init__.py
@backend_monitoramento/api/serializers/usinas.py

<interfaces>
<!-- Fixtures existentes no conftest.py que os testes reutilizam -->
From backend_monitoramento/api/tests/conftest.py:
```python
@pytest.fixture
def credencial(db): ...  # CredencialProvedor(provedor='solis')
@pytest.fixture
def usina(db, credencial): ...  # Usina(provedor='solis', ativo=True)
@pytest.fixture
def usina_hoymiles(db): ...  # Usina(provedor='hoymiles', ativo=True)
@pytest.fixture
def snapshot_usina(db, usina): ...  # SnapshotUsina(potencia_kw=5.5) + seta usina.ultimo_snapshot
@pytest.fixture
def inversor(db, usina): ...  # Inversor(usina=usina)
@pytest.fixture
def snapshot_inversor(db, inversor): ...  # SnapshotInversor(pac_kw=4.8) + seta inversor.ultimo_snapshot
@pytest.fixture
def tokens(client, db, django_user_model): ...  # {'access': '...', 'refresh': '...'}
```

From backend_monitoramento/usinas/models.py:
```python
class Usina(models.Model):
    # Campos relevantes para analytics:
    provedor = models.CharField(max_length=30)
    ativo = models.BooleanField(default=True)
    ultimo_snapshot = models.OneToOneField('SnapshotUsina', null=True, blank=True, ...)
    # NAO tem latitude/longitude — D-01 adiciona nesta task

class SnapshotUsina(models.Model):
    potencia_kw = models.FloatField(default=0.0)
    status = models.CharField(max_length=15, choices=STATUS_CHOICES, default='normal')

class Inversor(models.Model):
    usina = models.ForeignKey(Usina, on_delete=models.CASCADE, related_name='inversores')
    ultimo_snapshot = models.OneToOneField('SnapshotInversor', null=True, blank=True, ...)

class SnapshotInversor(models.Model):
    pac_kw = models.FloatField(default=0.0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration lat/lng + serializer de mapa + fixtures de analytics</name>
  <files>
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/serializers/analytics.py,
    backend_monitoramento/api/serializers/__init__.py,
    backend_monitoramento/api/tests/conftest.py
  </files>
  <read_first>
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/serializers/usinas.py,
    backend_monitoramento/api/serializers/__init__.py,
    backend_monitoramento/api/tests/conftest.py,
    .planning/phases/03-analytics-endpoints/03-CONTEXT.md,
    .planning/phases/03-analytics-endpoints/03-RESEARCH.md
  </read_first>
  <action>
1. Adicionar campos ao model Usina (per D-01) — ANTES do campo `ativo`:
   ```python
   latitude = models.FloatField(null=True, blank=True)
   longitude = models.FloatField(null=True, blank=True)
   ```

2. Gerar migration: `cd backend_monitoramento && python3 manage.py makemigrations usinas --name usina_add_lat_lng`
   Verificar que a migration gerada contem APENAS AddField para latitude e longitude.
   Aplicar: `python3 manage.py migrate usinas`

3. Criar `backend_monitoramento/api/serializers/analytics.py` com:
   ```python
   from rest_framework import serializers
   from usinas.models import Usina

   class UsinaMapaSerializer(serializers.ModelSerializer):
       status = serializers.SerializerMethodField()

       class Meta:
           model = Usina
           fields = ['id', 'nome', 'provedor', 'latitude', 'longitude', 'ativo', 'status']

       def get_status(self, obj) -> str:
           if obj.ultimo_snapshot is None:
               return 'sem_dados'
           return obj.ultimo_snapshot.status
   ```

4. Atualizar `backend_monitoramento/api/serializers/__init__.py` — adicionar:
   ```python
   from .analytics import UsinaMapaSerializer
   ```

5. Adicionar fixtures ao conftest.py (no final do arquivo):
   - `usina_com_coords(db, credencial)` — Usina com latitude=-23.5505, longitude=-46.6333, provedor='solis', ativo=True
   - `usina_sem_coords(db)` — Usina com latitude=None, longitude=None, provedor='hoymiles', ativo=True (criar nova credencial hoymiles inline)
   - `snapshot_usina_hoymiles(db, usina_hoymiles)` — SnapshotUsina(potencia_kw=3.0, status='normal') + seta usina_hoymiles.ultimo_snapshot
   - `inversor_hoymiles(db, usina_hoymiles)` — Inversor vinculado a usina_hoymiles
   - `snapshot_inversor_hoymiles(db, inversor_hoymiles)` — SnapshotInversor(pac_kw=2.5) + seta inversor_hoymiles.ultimo_snapshot
   - `inversor_inativo(db, usina)` — Inversor sem snapshot (ultimo_snapshot=None) — para testar exclusao do ranking
   - `inversor_pac_zero(db, usina)` — Inversor com snapshot mas pac_kw=0.0 — para testar exclusao do ranking

   IMPORTANTE: A fixture `usina_com_coords` deve usar a fixture `usina` existente e apenas setar os campos lat/lng via update, NAO criar nova usina. Isso evita conflito de fixtures.
   Alternativa melhor: nao criar fixture separada — atualizar diretamente dentro dos testes de mapa. Usar a fixture `usina` existente e setar coords no teste.
   
   Fixtures necessarias de verdade no conftest:
   - `snapshot_usina_hoymiles` (para ter 2 provedores com snapshot no teste de potencia)
   - `inversor_hoymiles` + `snapshot_inversor_hoymiles` (para ranking com 2 provedores)
   - `inversor_inativo` (Inversor sem snapshot, para testar exclusao)
   - `inversor_pac_zero` (Inversor com snapshot pac_kw=0, para testar exclusao)
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && python3 manage.py showmigrations usinas | grep -q "usina_add_lat_lng" && python3 -c "from usinas.models import Usina; print('latitude' in [f.name for f in Usina._meta.get_fields()])" && python3 -c "from api.serializers.analytics import UsinaMapaSerializer; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "latitude = models.FloatField(null=True, blank=True)" backend_monitoramento/usinas/models.py
    - grep -q "longitude = models.FloatField(null=True, blank=True)" backend_monitoramento/usinas/models.py
    - ls backend_monitoramento/usinas/migrations/*usina_add_lat_lng* retorna arquivo
    - grep -q "class UsinaMapaSerializer" backend_monitoramento/api/serializers/analytics.py
    - grep -q "from .analytics import UsinaMapaSerializer" backend_monitoramento/api/serializers/__init__.py
    - grep -q "inversor_inativo\|inversor_pac_zero\|snapshot_usina_hoymiles" backend_monitoramento/api/tests/conftest.py
  </acceptance_criteria>
  <done>
    Model Usina tem campos latitude/longitude, migration aplicada e reversivel, UsinaMapaSerializer exportado, fixtures de analytics adicionadas ao conftest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes RED para os 3 endpoints de analytics</name>
  <files>
    backend_monitoramento/api/tests/test_analytics.py
  </files>
  <read_first>
    backend_monitoramento/api/tests/conftest.py,
    .planning/phases/03-analytics-endpoints/03-RESEARCH.md,
    .planning/phases/03-analytics-endpoints/03-VALIDATION.md,
    backend_monitoramento/api/tests/test_garantias.py
  </read_first>
  <behavior>
    ## TestPotenciaMedia (ANA-01)
    - test_requer_auth: GET /api/analytics/potencia/ sem token retorna 401
    - test_media_geral: com 2 usinas (solis 5.5kW, hoymiles 3.0kW), media_geral_kw = 4.25
    - test_por_provedor: mesmo cenario, por_provedor contem 2 entradas com provedores corretos e medias individuais
    - test_sem_snapshots: nenhuma usina com snapshot, media_geral_kw = null, por_provedor = []
    - test_usina_sem_snapshot_excluida: 1 usina com snapshot + 1 sem, media so conta a com snapshot

    ## TestRankingFabricantes (ANA-02)
    - test_requer_auth: GET /api/analytics/ranking-fabricantes/ sem token retorna 401
    - test_ranking_ordenado: com inversores ativos de 2 provedores, retorna ordenado desc por inversores_ativos
    - test_top_5_limite: com mais de 5 provedores, retorna exatamente 5
    - test_exclui_sem_snapshot: inversor sem ultimo_snapshot nao conta como ativo
    - test_exclui_pac_zero: inversor com pac_kw=0 nao conta como ativo
    - test_sem_inversores: nenhum inversor, ranking = []

    ## TestMapaUsinas (ANA-03)
    - test_requer_auth: GET /api/analytics/mapa/ sem token retorna 401
    - test_retorna_todas_usinas: com 2 usinas, retorna lista com 2 itens
    - test_sem_coords_retorna_null: usina sem lat/lng retorna latitude=null, longitude=null (nao omitida)
    - test_com_coords: usina com lat/lng retorna valores corretos
    - test_sem_paginacao: com varias usinas, resposta e array direto (sem keys count/next/previous)
    - test_inclui_status: cada usina tem campo status (valor de ultimo_snapshot.status ou 'sem_dados')
  </behavior>
  <action>
Criar `backend_monitoramento/api/tests/test_analytics.py` com todas as classes de teste acima usando pytest + pytest-django.

Padrao a seguir (copiado dos testes existentes da Phase 2):
```python
import pytest
from django.utils import timezone
from usinas.models import Usina, SnapshotUsina, Inversor, SnapshotInversor

URL_POTENCIA = '/api/analytics/potencia/'
URL_RANKING = '/api/analytics/ranking-fabricantes/'
URL_MAPA = '/api/analytics/mapa/'


@pytest.mark.django_db
class TestPotenciaMedia:
    def test_requer_auth(self, client):
        resp = client.get(URL_POTENCIA)
        assert resp.status_code == 401

    def test_media_geral(self, client, tokens, usina, snapshot_usina, usina_hoymiles, snapshot_usina_hoymiles):
        # usina solis: potencia_kw=5.5, usina hoymiles: potencia_kw=3.0
        # media = (5.5 + 3.0) / 2 = 4.25
        resp = client.get(URL_POTENCIA, HTTP_AUTHORIZATION=f'Bearer {tokens["access"]}')
        assert resp.status_code == 200
        data = resp.json()
        assert data['media_geral_kw'] == pytest.approx(4.25, abs=0.01)
    # ... etc
```

Todos os testes devem FALHAR quando executados (endpoints /api/analytics/* ainda nao existem, retornam 404).
Nao criar stubs com @pytest.mark.skip — os testes devem falhar com 404, confirmando que as URLs nao existem.

Para o teste test_top_5_limite, criar 6+ provedores inline no teste (nao via fixture) com inversores ativos, e verificar que len(data['ranking']) == 5.

Para testes de mapa com coords, setar latitude/longitude diretamente no objeto usina dentro do teste:
```python
usina.latitude = -23.5505
usina.longitude = -46.6333
usina.save(update_fields=['latitude', 'longitude'])
```
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && python3 -m pytest api/tests/test_analytics.py -x --tb=line 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend_monitoramento/api/tests/test_analytics.py retorna >= 17
    - grep -q "class TestPotenciaMedia" backend_monitoramento/api/tests/test_analytics.py
    - grep -q "class TestRankingFabricantes" backend_monitoramento/api/tests/test_analytics.py
    - grep -q "class TestMapaUsinas" backend_monitoramento/api/tests/test_analytics.py
    - grep -q "URL_POTENCIA.*analytics/potencia" backend_monitoramento/api/tests/test_analytics.py
    - grep -q "URL_RANKING.*analytics/ranking" backend_monitoramento/api/tests/test_analytics.py
    - grep -q "URL_MAPA.*analytics/mapa" backend_monitoramento/api/tests/test_analytics.py
    - Todos os testes FALHAM (404) pois os endpoints ainda nao existem
  </acceptance_criteria>
  <done>
    Arquivo test_analytics.py criado com >= 17 testes cobrindo ANA-01, ANA-02 e ANA-03. Todos falham com 404 (endpoints nao implementados). Nenhum teste com skip ou stub vazio.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API analytics | Dados agregados de potencia, ranking e localizacao expostos via JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | /api/analytics/potencia/ | mitigate | IsAuthenticated global (DEFAULT_PERMISSION_CLASSES) — rejeita 401 sem token; teste test_requer_auth valida |
| T-3-02 | Information Disclosure | /api/analytics/ranking-fabricantes/ | mitigate | IsAuthenticated global — mesmo mecanismo de T-3-01 |
| T-3-03 | Information Disclosure | /api/analytics/mapa/ (lat/lng) | mitigate | IsAuthenticated global — coordenadas sao dados administrativos internos, protegidos por JWT; teste test_requer_auth valida |
| T-3-04 | Denial of Service | queries de aggregation sem limite | accept | Volume controlado (~centenas de usinas/inversores); queries com annotate sao O(n) no banco; sem risco com volume atual |
</threat_model>

<verification>
1. Migration `usina_add_lat_lng` existe e e reversivel: `python3 manage.py migrate usinas 0004 && python3 manage.py migrate usinas`
2. UsinaMapaSerializer importavel: `python3 -c "from api.serializers.analytics import UsinaMapaSerializer"`
3. Testes existem e falham com 404: `pytest api/tests/test_analytics.py` mostra falhas (nao erros de import)
4. Suite existente continua verde: `pytest api/tests/ -x --ignore=api/tests/test_analytics.py`
</verification>

<success_criteria>
- Campos latitude/longitude existem no model Usina com null=True, blank=True
- Migration reversivel aplicada ao banco
- UsinaMapaSerializer criado e exportado via __init__.py
- Fixtures de analytics (snapshot_usina_hoymiles, inversor_hoymiles, etc.) no conftest.py
- test_analytics.py com >= 17 testes cobrindo todos os comportamentos de ANA-01/02/03
- Testes FALHAM com 404 (confirmando que os endpoints serao criados no Plan 02)
- Suite existente (test_auth, test_usinas, test_garantias) continua passando
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-endpoints/03-01-SUMMARY.md`
</output>
