---
phase: 01-api-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend_monitoramento/api/tests/__init__.py
  - backend_monitoramento/api/tests/test_auth.py
  - backend_monitoramento/api/tests/test_cors.py
  - backend_monitoramento/usinas/tests/__init__.py
  - backend_monitoramento/usinas/tests/test_garantia.py
autonomous: true
requirements:
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - GAR-01

must_haves:
  truths:
    - "POST /api/auth/token/ com credenciais validas retorna access e refresh tokens"
    - "POST /api/auth/token/ com credenciais invalidas retorna 401"
    - "POST /api/auth/token/refresh/ com refresh valido emite novo access token"
    - "Refresh token original nao e reutilizavel apos rotacao (blacklist ativa)"
    - "GET /api/ping/ sem token retorna 401"
    - "CORS bloqueia origem nao configurada"
    - "CORS permite origem configurada"
    - "GarantiaUsina.data_fim calcula corretamente a data de fim"
    - "GarantiaUsina.ativa retorna True dentro do prazo e False quando vencida"
  artifacts:
    - path: "backend_monitoramento/api/tests/test_auth.py"
      provides: "Testes de autenticacao JWT"
      contains: "test_login_retorna_tokens"
    - path: "backend_monitoramento/api/tests/test_cors.py"
      provides: "Testes de CORS"
      contains: "test_cors_bloqueia_origem_invalida"
    - path: "backend_monitoramento/usinas/tests/test_garantia.py"
      provides: "Testes do model GarantiaUsina"
      contains: "test_garantia_data_fim_calculada"
  key_links:
    - from: "backend_monitoramento/api/tests/test_auth.py"
      to: "/api/auth/token/"
      via: "client.post com credenciais"
      pattern: "reverse.*token_obtain_pair"
    - from: "backend_monitoramento/api/tests/test_cors.py"
      to: "CorsMiddleware"
      via: "HTTP_ORIGIN header em OPTIONS request"
      pattern: "HTTP_ORIGIN"
    - from: "backend_monitoramento/usinas/tests/test_garantia.py"
      to: "GarantiaUsina"
      via: "instanciacao direta do model"
      pattern: "GarantiaUsina"
---

<objective>
Escrever todos os testes da Phase 1: autenticacao JWT (login, refresh, rotacao, protecao 401), CORS (bloqueio e permissao) e GarantiaUsina (data_fim, ativa, dias_restantes).

Purpose: Validar que toda a infraestrutura configurada no Plan 01 funciona conforme os requisitos. Estes testes sao o gate de qualidade da fase.
Output: Suite de testes verde cobrindo API-02 a API-06 e GAR-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-infrastructure/01-RESEARCH.md
@.planning/phases/01-api-infrastructure/01-VALIDATION.md
@.planning/phases/01-api-infrastructure/01-01-SUMMARY.md

@backend_monitoramento/config/settings/base.py
@backend_monitoramento/api/urls.py
@backend_monitoramento/api/views.py
@backend_monitoramento/usinas/models.py
@backend_monitoramento/pytest.ini

<interfaces>
<!-- From Plan 01 outputs -->
From backend_monitoramento/api/urls.py:
```python
urlpatterns = [
    path('auth/token/', TokenObtainPairView.as_view(), name='token_obtain_pair'),
    path('auth/token/refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    path('ping/', PingView.as_view(), name='api_ping'),
]
```

From backend_monitoramento/usinas/models.py (GarantiaUsina):
```python
class GarantiaUsina(models.Model):
    usina = models.OneToOneField(Usina, on_delete=models.CASCADE, related_name='garantia')
    data_inicio = models.DateField()
    meses = models.PositiveIntegerField()
    observacoes = models.TextField(blank=True)

    @property
    def data_fim(self): ...      # data_inicio + relativedelta(months=meses)
    @property
    def ativa(self): ...         # data_fim >= timezone.now().date()
    @property
    def dias_restantes(self): ...  # max((data_fim - today).days, 0)
```

From backend_monitoramento/config/settings/base.py:
```python
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': ['rest_framework_simplejwt.authentication.JWTAuthentication'],
    'DEFAULT_PERMISSION_CLASSES': ['rest_framework.permissions.IsAuthenticated'],
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20,
}
SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=15),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
    'AUTH_HEADER_TYPES': ('Bearer',),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de autenticacao JWT (login, refresh, rotacao, protecao 401)</name>
  <files>
    backend_monitoramento/api/tests/__init__.py,
    backend_monitoramento/api/tests/test_auth.py
  </files>
  <read_first>
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/views.py,
    backend_monitoramento/config/settings/base.py,
    backend_monitoramento/pytest.ini,
    .planning/phases/01-api-infrastructure/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_login_retorna_tokens: POST /api/auth/token/ com username='admin', password='senha123' retorna 200 com chaves 'access' e 'refresh' no JSON
    - test_login_credenciais_invalidas: POST /api/auth/token/ com password errada retorna 401
    - test_refresh_emite_novo_access: POST /api/auth/token/refresh/ com refresh token valido retorna 200 com chave 'access' no JSON
    - test_refresh_token_rotacionado_invalido: Apos usar refresh uma vez, usar o mesmo refresh novamente retorna 401 (blacklist ativa)
    - test_endpoint_protegido_sem_token: GET /api/ping/ sem header Authorization retorna 401
    - test_endpoint_protegido_com_token: GET /api/ping/ com header Authorization: Bearer {access_token} retorna 200 com {'status': 'ok'}
    - test_token_lifetimes: Decodificar access token e verificar que exp - iat == 900 (15 min); decodificar refresh e verificar exp - iat == 604800 (7 dias)
  </behavior>
  <action>
1. Criar `backend_monitoramento/api/tests/__init__.py` (arquivo vazio).

2. Criar `backend_monitoramento/api/tests/test_auth.py` com os seguintes testes:

```python
import pytest
import json
import base64
from django.urls import reverse


@pytest.mark.django_db
class TestAutenticacaoJWT:
    """Testes de autenticacao JWT — cobre API-02, API-03, API-04, API-06."""

    @pytest.fixture
    def usuario(self, django_user_model):
        return django_user_model.objects.create_user(
            username='admin', password='senha123'
        )

    @pytest.fixture
    def tokens(self, client, usuario):
        response = client.post(
            reverse('token_obtain_pair'),
            {'username': 'admin', 'password': 'senha123'},
            content_type='application/json',
        )
        return response.json()

    def test_login_retorna_tokens(self, client, usuario):
        """API-02: Login com credenciais validas retorna access e refresh."""
        response = client.post(
            reverse('token_obtain_pair'),
            {'username': 'admin', 'password': 'senha123'},
            content_type='application/json',
        )
        assert response.status_code == 200
        data = response.json()
        assert 'access' in data
        assert 'refresh' in data

    def test_login_credenciais_invalidas(self, client, usuario):
        """API-02: Login com credenciais invalidas retorna 401."""
        response = client.post(
            reverse('token_obtain_pair'),
            {'username': 'admin', 'password': 'errada'},
            content_type='application/json',
        )
        assert response.status_code == 401

    def test_refresh_emite_novo_access(self, client, tokens):
        """API-03: Refresh valido emite novo access token."""
        response = client.post(
            reverse('token_refresh'),
            {'refresh': tokens['refresh']},
            content_type='application/json',
        )
        assert response.status_code == 200
        assert 'access' in response.json()

    def test_refresh_token_rotacionado_invalido(self, client, tokens):
        """API-03/API-06: Token original nao e reutilizavel apos rotacao."""
        # Primeiro uso — deve funcionar
        response1 = client.post(
            reverse('token_refresh'),
            {'refresh': tokens['refresh']},
            content_type='application/json',
        )
        assert response1.status_code == 200

        # Segundo uso do MESMO refresh — deve falhar (blacklist)
        response2 = client.post(
            reverse('token_refresh'),
            {'refresh': tokens['refresh']},
            content_type='application/json',
        )
        assert response2.status_code == 401

    def test_endpoint_protegido_sem_token(self, client):
        """API-04: Endpoint protegido rejeita requisicao sem token."""
        response = client.get(reverse('api_ping'))
        assert response.status_code == 401

    def test_endpoint_protegido_com_token(self, client, tokens):
        """API-04: Endpoint protegido aceita requisicao com token valido."""
        response = client.get(
            reverse('api_ping'),
            HTTP_AUTHORIZATION=f"Bearer {tokens['access']}",
        )
        assert response.status_code == 200
        assert response.json() == {'status': 'ok'}

    def test_token_lifetimes(self, tokens):
        """API-06: Access token expira em 15 min; refresh em 7 dias."""
        def decode_payload(token):
            payload = token.split('.')[1]
            # Adiciona padding para base64
            payload += '=' * (4 - len(payload) % 4)
            return json.loads(base64.urlsafe_b64decode(payload))

        access_payload = decode_payload(tokens['access'])
        assert access_payload['exp'] - access_payload['iat'] == 900  # 15 min

        refresh_payload = decode_payload(tokens['refresh'])
        assert refresh_payload['exp'] - refresh_payload['iat'] == 604800  # 7 dias
```

3. Rodar os testes: `cd backend_monitoramento && pytest api/tests/test_auth.py -x -v`
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && pytest api/tests/test_auth.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `api/tests/__init__.py` existe
    - `api/tests/test_auth.py` contem `class TestAutenticacaoJWT`
    - `api/tests/test_auth.py` contem `def test_login_retorna_tokens`
    - `api/tests/test_auth.py` contem `def test_login_credenciais_invalidas`
    - `api/tests/test_auth.py` contem `def test_refresh_emite_novo_access`
    - `api/tests/test_auth.py` contem `def test_refresh_token_rotacionado_invalido`
    - `api/tests/test_auth.py` contem `def test_endpoint_protegido_sem_token`
    - `api/tests/test_auth.py` contem `def test_endpoint_protegido_com_token`
    - `api/tests/test_auth.py` contem `def test_token_lifetimes`
    - `pytest api/tests/test_auth.py -x` retorna exit code 0 (todos os 7 testes passam)
  </acceptance_criteria>
  <done>7 testes de autenticacao JWT passando: login valido/invalido, refresh, rotacao com blacklist, protecao 401 sem/com token, lifetimes corretos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes de CORS e GarantiaUsina</name>
  <files>
    backend_monitoramento/api/tests/test_cors.py,
    backend_monitoramento/usinas/tests/__init__.py,
    backend_monitoramento/usinas/tests/test_garantia.py
  </files>
  <read_first>
    backend_monitoramento/config/settings/base.py,
    backend_monitoramento/usinas/models.py,
    .planning/phases/01-api-infrastructure/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_cors_bloqueia_origem_invalida: OPTIONS /api/auth/token/ com HTTP_ORIGIN='http://atacante.com' NAO contem header 'Access-Control-Allow-Origin' na resposta
    - test_cors_permite_origem_valida: OPTIONS /api/auth/token/ com HTTP_ORIGIN='http://localhost:5173' contem header 'Access-Control-Allow-Origin' = 'http://localhost:5173'
    - test_garantia_data_fim_calculada: GarantiaUsina(data_inicio=date(2024,1,15), meses=12).data_fim == date(2025,1,15)
    - test_garantia_data_fim_fim_de_mes: GarantiaUsina(data_inicio=date(2024,1,31), meses=1).data_fim == date(2024,2,29) (ano bissexto)
    - test_garantia_ativa_quando_dentro_do_prazo: GarantiaUsina(data_inicio=date(2025,1,1), meses=120).ativa is True
    - test_garantia_inativa_quando_vencida: GarantiaUsina(data_inicio=date(2020,1,1), meses=12).ativa is False
    - test_garantia_dias_restantes_positivo: GarantiaUsina com prazo futuro retorna dias_restantes > 0
    - test_garantia_dias_restantes_zero_quando_vencida: GarantiaUsina vencida retorna dias_restantes == 0
    - test_garantia_str: str(garantia) contem nome da usina e meses
  </behavior>
  <action>
1. Criar `backend_monitoramento/api/tests/test_cors.py`:

```python
import pytest
from django.urls import reverse


@pytest.mark.django_db
class TestCORS:
    """Testes de CORS — cobre API-05."""

    def test_cors_bloqueia_origem_invalida(self, client, settings):
        """API-05: Origem nao listada nao recebe header CORS."""
        settings.CORS_ALLOWED_ORIGINS = ['http://painel.firmasolar.com.br']
        response = client.options(
            reverse('token_obtain_pair'),
            HTTP_ORIGIN='http://atacante.com',
            HTTP_ACCESS_CONTROL_REQUEST_METHOD='POST',
        )
        assert 'Access-Control-Allow-Origin' not in response

    def test_cors_permite_origem_valida(self, client, settings):
        """API-05: Origem configurada recebe header CORS correto."""
        settings.CORS_ALLOWED_ORIGINS = ['http://localhost:5173']
        response = client.options(
            reverse('token_obtain_pair'),
            HTTP_ORIGIN='http://localhost:5173',
            HTTP_ACCESS_CONTROL_REQUEST_METHOD='POST',
        )
        assert response['Access-Control-Allow-Origin'] == 'http://localhost:5173'
```

2. Criar `backend_monitoramento/usinas/tests/__init__.py` (arquivo vazio, se nao existir).

3. Criar `backend_monitoramento/usinas/tests/test_garantia.py`:

```python
import pytest
from datetime import date
from unittest.mock import patch
from django.utils import timezone as dj_timezone
from usinas.models import GarantiaUsina, Usina


@pytest.fixture
def usina(db):
    """Cria uma usina minima para testes de garantia."""
    from provedores.models import CredencialProvedor
    cred = CredencialProvedor.objects.create(
        provedor='solis',
        identificador='test',
        credenciais_enc=b'test',
    )
    return Usina.objects.create(
        id_usina_provedor='test-001',
        provedor='solis',
        credencial=cred,
        nome='Usina Teste',
        capacidade_kwp=10.0,
    )


class TestGarantiaUsina:
    """Testes do model GarantiaUsina — cobre GAR-01."""

    def test_garantia_data_fim_calculada(self, usina):
        """GAR-01: data_fim = data_inicio + meses."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2024, 1, 15),
            meses=12,
        )
        assert garantia.data_fim == date(2025, 1, 15)

    def test_garantia_data_fim_fim_de_mes(self, usina):
        """GAR-01: data_fim lida corretamente com fim de mes (bissexto)."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2024, 1, 31),
            meses=1,
        )
        # relativedelta(months=1) de 31/jan/2024 = 29/fev/2024 (bissexto)
        assert garantia.data_fim == date(2024, 2, 29)

    def test_garantia_ativa_quando_dentro_do_prazo(self, usina):
        """GAR-01: ativa retorna True quando data_fim esta no futuro."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2025, 1, 1),
            meses=120,  # 10 anos
        )
        assert garantia.ativa is True

    def test_garantia_inativa_quando_vencida(self, usina):
        """GAR-01: ativa retorna False quando data_fim esta no passado."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2020, 1, 1),
            meses=12,
        )
        assert garantia.ativa is False

    def test_garantia_dias_restantes_positivo(self, usina):
        """GAR-01: dias_restantes > 0 quando garantia ativa."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2025, 1, 1),
            meses=120,
        )
        assert garantia.dias_restantes > 0

    def test_garantia_dias_restantes_zero_quando_vencida(self, usina):
        """GAR-01: dias_restantes == 0 quando garantia vencida."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2020, 1, 1),
            meses=12,
        )
        assert garantia.dias_restantes == 0

    def test_garantia_str(self, usina):
        """GAR-01: __str__ contem nome da usina e meses."""
        garantia = GarantiaUsina(
            usina=usina,
            data_inicio=date(2024, 1, 1),
            meses=24,
        )
        resultado = str(garantia)
        assert 'Usina Teste' in resultado
        assert '24' in resultado

    @pytest.mark.django_db
    def test_garantia_persistencia(self, usina):
        """GAR-01: GarantiaUsina persiste e recupera do banco."""
        GarantiaUsina.objects.create(
            usina=usina,
            data_inicio=date(2024, 6, 1),
            meses=36,
            observacoes='Garantia de teste',
        )
        garantia = GarantiaUsina.objects.get(usina=usina)
        assert garantia.meses == 36
        assert garantia.data_fim == date(2027, 6, 1)
        assert garantia.observacoes == 'Garantia de teste'
```

NOTA sobre a fixture `usina`: verificar o model `CredencialProvedor` em `provedores/models.py` antes de implementar. O executor deve ler o model real e ajustar os campos da fixture conforme necessario. Os campos `identificador` e `credenciais_enc` sao estimados — confirmar via `read_first`.

4. Rodar a suite completa: `cd backend_monitoramento && pytest api/tests/ usinas/tests/test_garantia.py -x -v`
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && pytest api/tests/ usinas/tests/test_garantia.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `api/tests/test_cors.py` contem `class TestCORS`
    - `api/tests/test_cors.py` contem `def test_cors_bloqueia_origem_invalida`
    - `api/tests/test_cors.py` contem `def test_cors_permite_origem_valida`
    - `usinas/tests/__init__.py` existe
    - `usinas/tests/test_garantia.py` contem `class TestGarantiaUsina`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_data_fim_calculada`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_data_fim_fim_de_mes`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_ativa_quando_dentro_do_prazo`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_inativa_quando_vencida`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_dias_restantes_positivo`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_dias_restantes_zero_quando_vencida`
    - `usinas/tests/test_garantia.py` contem `def test_garantia_persistencia`
    - `pytest api/tests/ usinas/tests/test_garantia.py -x` retorna exit code 0 (todos os testes passam)
  </acceptance_criteria>
  <done>Suite completa verde: 2 testes CORS + 8 testes GarantiaUsina + 7 testes auth = 17 testes cobrindo todos os requisitos da Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client -> API | Testes simulam requisicoes HTTP; verificam que boundaries de seguranca funcionam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Repudiation | TokenRefreshView | mitigate | Teste `test_refresh_token_rotacionado_invalido` valida que blacklist impede reuso |
| T-1-02 | Tampering | CorsMiddleware | mitigate | Teste `test_cors_bloqueia_origem_invalida` valida que origens nao listadas sao bloqueadas |
| T-1-04 | Spoofing | /api/auth/token/ | mitigate | Teste `test_login_credenciais_invalidas` valida rejeicao com 401 |
| T-1-05 | Elevation of Privilege | /api/ping/ | mitigate | Teste `test_endpoint_protegido_sem_token` valida que endpoint sem token retorna 401 |
</threat_model>

<verification>
1. `cd backend_monitoramento && pytest api/tests/ usinas/tests/test_garantia.py -x -v` — todos os testes passam
2. `cd backend_monitoramento && pytest -x -q` — suite completa verde (incluindo testes existentes)
3. Cobertura de requirements: API-02 (2 testes), API-03 (2 testes), API-04 (2 testes), API-05 (2 testes), API-06 (1 teste), GAR-01 (8 testes)
</verification>

<success_criteria>
- 17 testes escritos e passando: 7 auth, 2 CORS, 8 GarantiaUsina
- Cada requisito da Phase 1 tem ao menos 1 teste automatizado
- Suite completa (`pytest -x -q`) verde sem regressoes nos testes existentes
- Testes validam comportamento (nao implementacao) conforme CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-infrastructure/01-02-SUMMARY.md`
</output>
