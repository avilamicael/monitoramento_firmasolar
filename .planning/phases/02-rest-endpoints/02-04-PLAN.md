---
phase: 02-rest-endpoints
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - backend_monitoramento/api/serializers/logs.py
  - backend_monitoramento/api/views/logs.py
  - backend_monitoramento/api/urls.py
  - backend_monitoramento/api/tests/test_logs.py
autonomous: true
requirements:
  - LOG-01
---

# Plan 04: Logs de Coleta

**Phase goal contribution:** Endpoint `GET /api/coleta/logs/` expõe os últimos ciclos de coleta com status e timestamp, completando a cobertura de dados operacionais da fase.

<must_haves>
- `GET /api/coleta/logs/` retorna lista dos últimos ciclos de coleta com `status` e `iniciado_em`
- Endpoint retorna 401 sem token JWT válido
- Suite completa verde: `pytest api/tests/ --tb=short` exits 0
</must_haves>

<threat_model>
  <threat id="T-2-11" severity="high">
    <description>Endpoint de logs acessível sem autenticação — expõe informações de infraestrutura (timestamps de coleta, erros internos)</description>
    <mitigation>LogColetaListView usa `permission_classes = [IsAuthenticated]`. Teste explícito: GET /api/coleta/logs/ sem token retorna 401.</mitigation>
  </threat>
  <threat id="T-2-12" severity="medium">
    <description>Campo `detalhe_erro` pode expor stack traces internos</description>
    <mitigation>Campo incluído para admins autenticados — log de auditoria é o propósito declarado. Acessível apenas com JWT válido.</mitigation>
  </threat>
  <threat id="T-2-13" severity="low">
    <description>Listagem sem paginação pode retornar volume excessivo</description>
    <mitigation>`pagination_class = PaginacaoSnapshots` (page_size=100). `select_related('credencial')` evita N+1.</mitigation>
  </threat>
</threat_model>

## Wave 4

### Task 04-01 — Serializer de LogColeta

<task id="04-01">
<title>Criar LogColetaSerializer</title>

<read_first>
- backend_monitoramento/coleta/models.py (campos de LogColeta: id, credencial FK, status, usinas_coletadas, inversores_coletados, alertas_sincronizados, detalhe_erro, duracao_ms, iniciado_em)
- backend_monitoramento/api/serializers/garantias.py (padrão de serializer da fase)
- backend_monitoramento/api/serializers/__init__.py (registro de imports)
</read_first>

<action>
Criar `backend_monitoramento/api/serializers/logs.py`:

```python
from rest_framework import serializers
from coleta.models import LogColeta


class LogColetaSerializer(serializers.ModelSerializer):
    # credencial.provedor é CharField com choices (ex: 'solis'), não FK.
    # get_provedor_display() retorna o label legível ('Solis Cloud', etc.)
    provedor_nome = serializers.SerializerMethodField()

    def get_provedor_nome(self, obj) -> str:
        return obj.credencial.get_provedor_display()

    class Meta:
        model = LogColeta
        fields = [
            'id',
            'provedor_nome',
            'status',
            'usinas_coletadas',
            'inversores_coletados',
            'alertas_sincronizados',
            'detalhe_erro',
            'duracao_ms',
            'iniciado_em',
        ]
        read_only_fields = fields
```

Adicionar import em `backend_monitoramento/api/serializers/__init__.py`:
```python
from .logs import LogColetaSerializer
```
</action>

<acceptance_criteria>
- `api/serializers/logs.py` existe e contém `class LogColetaSerializer`
- `api/serializers/logs.py` contém `fields = [` com `'id'`, `'status'`, `'iniciado_em'`
- `api/serializers/logs.py` contém `provedor_nome` com `SerializerMethodField` e `get_provedor_nome`
- `api/serializers/__init__.py` contém `from .logs import LogColetaSerializer`
- `api/serializers/logs.py` NÃO contém `payload_bruto`
</acceptance_criteria>
</task>

### Task 04-02 — View de LogColeta

<task id="04-02">
<title>Criar LogColetaListView</title>

<read_first>
- backend_monitoramento/api/views/garantias.py (padrão de ListAPIView da fase)
- backend_monitoramento/api/pagination.py (PaginacaoSnapshots — criada no Plan 01)
- backend_monitoramento/api/serializers/logs.py (recém-criado)
- backend_monitoramento/coleta/models.py (LogColeta, ordering: -iniciado_em)
</read_first>

<action>
Criar `backend_monitoramento/api/views/logs.py`:

```python
from rest_framework.generics import ListAPIView
from rest_framework.permissions import IsAuthenticated

from coleta.models import LogColeta
from api.serializers import LogColetaSerializer
from api.pagination import PaginacaoSnapshots


class LogColetaListView(ListAPIView):
    """
    GET /api/coleta/logs/
    Retorna últimos ciclos de coleta com status e timestamp.
    Ordenação: mais recente primeiro (definida pelo Meta.ordering do model).
    """
    serializer_class = LogColetaSerializer
    permission_classes = [IsAuthenticated]
    pagination_class = PaginacaoSnapshots  # criada no Plan 01; page_size=100

    def get_queryset(self):
        # select_related('credencial') evita N+1 em get_provedor_display()
        # credencial.provedor é CharField — sem join extra necessário
        return LogColeta.objects.select_related('credencial').all()
```

Adicionar import em `backend_monitoramento/api/views/__init__.py`:
```python
from .logs import LogColetaListView
```
</action>

<acceptance_criteria>
- `api/views/logs.py` existe e contém `class LogColetaListView`
- `api/views/logs.py` contém `permission_classes = [IsAuthenticated]`
- `api/views/logs.py` contém `select_related('credencial')`
- `api/views/__init__.py` contém `from .logs import LogColetaListView`
</acceptance_criteria>
</task>

### Task 04-03 — Wiring de URL

<task id="04-03">
<title>Registrar rota GET /api/coleta/logs/</title>

<read_first>
- backend_monitoramento/api/urls.py (rotas existentes — padrão de roteamento das demais views da fase)
- backend_monitoramento/api/views/logs.py (recém-criado)
</read_first>

<action>
Abrir `backend_monitoramento/api/urls.py` e adicionar a rota de logs.

O endpoint é uma `ListAPIView` (não um ViewSet), portanto usa `path` diretamente, não `router.register`:

```python
from api.views import LogColetaListView

# Adicionar dentro do urlpatterns existente:
path('coleta/logs/', LogColetaListView.as_view(), name='log-coleta-list'),
```

A rota DEVE ser registrada como `coleta/logs/` (sem prefixo `/api/` — o prefixo é adicionado no urls.py raiz via `include('api.urls', namespace='api')`).
</action>

<acceptance_criteria>
- `api/urls.py` contém `coleta/logs/`
- `api/urls.py` contém `LogColetaListView.as_view()`
</acceptance_criteria>
</task>

### Task 04-04 — Implementar test_logs.py

<task id="04-04">
<title>Implementar testes para GET /api/coleta/logs/</title>

<read_first>
- backend_monitoramento/api/tests/conftest.py (fixtures disponíveis: `api_client`, `token_usuario`, `credencial`)
- backend_monitoramento/api/tests/test_usinas.py (padrão de teste de listagem e auth da fase)
- backend_monitoramento/coleta/models.py (campos de LogColeta para montar fixture)
</read_first>

<action>
Implementar os stubs em `backend_monitoramento/api/tests/test_logs.py` (stub criado no Plan 01):

```python
import pytest
from django.urls import reverse
from coleta.models import LogColeta


@pytest.mark.django_db
class TestLogColetaList:

    def test_lista_logs_autenticado(self, api_client, token_usuario, credencial):
        """LOG-01: lista logs com status e timestamp"""
        LogColeta.objects.create(
            credencial=credencial,
            status='sucesso',
            usinas_coletadas=3,
            inversores_coletados=9,
            alertas_sincronizados=0,
            duracao_ms=1500,
        )
        LogColeta.objects.create(
            credencial=credencial,
            status='erro',
            usinas_coletadas=0,
            inversores_coletados=0,
            alertas_sincronizados=0,
            duracao_ms=200,
            detalhe_erro='Timeout na API do provedor',
        )

        api_client.credentials(HTTP_AUTHORIZATION=f'Bearer {token_usuario}')
        url = reverse('api:log-coleta-list')
        response = api_client.get(url)

        assert response.status_code == 200
        data = response.json()
        assert data['count'] == 2
        results = data['results']
        # Ordenação: mais recente primeiro
        assert results[0]['status'] == 'erro'
        assert results[0]['iniciado_em'] is not None
        assert results[1]['status'] == 'sucesso'
        assert 'provedor_nome' in results[0]

    def test_lista_logs_sem_token_retorna_401(self, api_client):
        """Todos os endpoints retornam 401 sem token — sem exceções"""
        url = reverse('api:log-coleta-list')
        response = api_client.get(url)
        assert response.status_code in [401, 403]

    def test_lista_logs_vazia(self, api_client, token_usuario):
        """Caso de borda: sem logs ainda"""
        api_client.credentials(HTTP_AUTHORIZATION=f'Bearer {token_usuario}')
        url = reverse('api:log-coleta-list')
        response = api_client.get(url)
        assert response.status_code == 200
        assert response.json()['count'] == 0

    def test_detalhe_erro_presente_quando_preenchido(self, api_client, token_usuario, credencial):
        """Campo detalhe_erro aparece quando há erro"""
        LogColeta.objects.create(
            credencial=credencial,
            status='erro',
            detalhe_erro='Connection refused',
        )
        api_client.credentials(HTTP_AUTHORIZATION=f'Bearer {token_usuario}')
        url = reverse('api:log-coleta-list')
        response = api_client.get(url)
        assert response.status_code == 200
        resultado = response.json()['results'][0]
        assert resultado['detalhe_erro'] == 'Connection refused'
```
</action>

<acceptance_criteria>
- `api/tests/test_logs.py` contém `class TestLogColetaList`
- `api/tests/test_logs.py` contém `test_lista_logs_sem_token_retorna_401`
- `api/tests/test_logs.py` contém `reverse('api:log-coleta-list')`
- `cd backend_monitoramento && pytest api/tests/test_logs.py -x --tb=short` exits 0
- `cd backend_monitoramento && pytest api/tests/ --tb=short` exits 0 (suite completa)
</acceptance_criteria>
</task>

<verification>
1. `cd backend_monitoramento && pytest api/tests/test_logs.py -x --tb=short` — todos passam
2. `cd backend_monitoramento && pytest api/tests/ --tb=short` — suite inteira verde
3. `grep -r "coleta/logs" backend_monitoramento/api/urls.py` — rota registrada
4. `grep "LogColetaListView" backend_monitoramento/api/views/logs.py` — view existe
5. Nenhum serializer em `api/serializers/logs.py` contém `payload_bruto`
</verification>

<success_criteria>
- GET /api/coleta/logs/ retorna lista paginada com `status` e `iniciado_em` por log
- Endpoint retorna 401/403 sem token JWT válido
- `provedor_nome` aparece em cada resultado via `get_provedor_display()`
- Suite completa de testes verde (incluindo os 4 novos testes de logs)
- LOG-01 completamente implementado e testado
</success_criteria>

<output>
After completion, create `.planning/phases/02-rest-endpoints/02-04-SUMMARY.md`
</output>
