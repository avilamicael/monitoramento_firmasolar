---
phase: 02-rest-endpoints
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - backend_monitoramento/api/serializers/inversores.py
  - backend_monitoramento/api/serializers/alertas.py
  - backend_monitoramento/api/filters/inversores.py
  - backend_monitoramento/api/filters/alertas.py
  - backend_monitoramento/api/views/inversores.py
  - backend_monitoramento/api/views/alertas.py
  - backend_monitoramento/api/urls.py
  - backend_monitoramento/api/tests/test_inversores.py
  - backend_monitoramento/api/tests/test_alertas.py
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - ALT-01
  - ALT-02
  - ALT-03
  - ALT-04

must_haves:
  truths:
    - "Administrador lista inversores filtrados por usina, provedor e modelo"
    - "Detalhe de inversor retorna ultimo snapshot completo (potencia, tensao, corrente, temperatura, frequencia)"
    - "Historico de snapshots de inversor paginado com page_size=100"
    - "Administrador lista alertas filtrados por estado, nivel e usina"
    - "Detalhe de alerta retorna dados completos incluindo com_garantia"
    - "PATCH em alerta atualiza estado e anotacoes; POST e DELETE retornam 405"
    - "Campo com_garantia presente em todas as respostas de alerta (lista e detalhe)"
    - "Todos os endpoints retornam 401 sem token"
  artifacts:
    - path: "backend_monitoramento/api/serializers/inversores.py"
      provides: "InversorListSerializer, InversorDetalheSerializer, SnapshotInversorSerializer"
      min_lines: 40
    - path: "backend_monitoramento/api/serializers/alertas.py"
      provides: "AlertaListSerializer, AlertaDetalheSerializer, AlertaPatchSerializer"
      min_lines: 40
    - path: "backend_monitoramento/api/views/inversores.py"
      provides: "InversorViewSet com action snapshots"
      contains: "class InversorViewSet"
    - path: "backend_monitoramento/api/views/alertas.py"
      provides: "AlertaViewSet com campo com_garantia"
      contains: "class AlertaViewSet"
  key_links:
    - from: "backend_monitoramento/api/views/alertas.py"
      to: "backend_monitoramento/usinas/models.py"
      via: "select_related para com_garantia"
      pattern: "select_related.*usina__garantia"
    - from: "backend_monitoramento/api/urls.py"
      to: "backend_monitoramento/api/views/inversores.py"
      via: "router.register"
      pattern: "router.register.*inversores.*InversorViewSet"
    - from: "backend_monitoramento/api/urls.py"
      to: "backend_monitoramento/api/views/alertas.py"
      via: "router.register"
      pattern: "router.register.*alertas.*AlertaViewSet"
---

<objective>
Implementar os endpoints de Inversores (list, detail, snapshots) e Alertas (list, detail, patch com campo com_garantia) com serializers, filters, views, URL wiring e testes completos.

Purpose: Entregar INV-01..03 e ALT-01..04 — o administrador consegue visualizar inversores com snapshots e gerir alertas via API.
Output: ViewSets, serializers, filtros e testes verdes para inversores e alertas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rest-endpoints/02-CONTEXT.md
@.planning/phases/02-rest-endpoints/02-RESEARCH.md
@.planning/phases/02-rest-endpoints/02-02-SUMMARY.md

<interfaces>
<!-- Models relevantes -->

From backend_monitoramento/usinas/models.py:
```python
class Inversor(models.Model):
    id = models.UUIDField(primary_key=True)
    usina = models.ForeignKey(Usina, related_name='inversores')
    id_inversor_provedor = models.CharField(max_length=200)
    numero_serie = models.CharField(max_length=100, blank=True)
    modelo = models.CharField(max_length=100, blank=True)
    ultimo_snapshot = models.OneToOneField('SnapshotInversor', null=True, blank=True, related_name='+')
    criado_em = models.DateTimeField(auto_now_add=True)

class SnapshotInversor(models.Model):
    id = models.UUIDField(primary_key=True)
    inversor = models.ForeignKey(Inversor, related_name='snapshots')
    coletado_em = models.DateTimeField()
    estado = models.CharField(max_length=10)  # normal|aviso|offline
    pac_kw = models.FloatField()
    energia_hoje_kwh = models.FloatField()
    energia_total_kwh = models.FloatField()
    soc_bateria = models.FloatField(null=True)
    strings_mppt = models.JSONField()
    tensao_ac_v = models.FloatField(null=True)
    corrente_ac_a = models.FloatField(null=True)
    tensao_dc_v = models.FloatField(null=True)
    corrente_dc_a = models.FloatField(null=True)
    frequencia_hz = models.FloatField(null=True)
    temperatura_c = models.FloatField(null=True)
    payload_bruto = models.JSONField()  # NUNCA incluir em serializers
```

From backend_monitoramento/alertas/models.py:
```python
class Alerta(models.Model):
    ESTADO_CHOICES = [('ativo', 'Ativo'), ('em_atendimento', 'Em atendimento'), ('resolvido', 'Resolvido')]
    NIVEL_CHOICES = [('info', 'Info'), ('aviso', 'Aviso'), ('importante', 'Importante'), ('critico', 'Critico')]

    id = models.UUIDField(primary_key=True)
    usina = models.ForeignKey('usinas.Usina', related_name='alertas')
    catalogo_alarme = models.ForeignKey(CatalogoAlarme, null=True, blank=True, related_name='alertas')
    id_alerta_provedor = models.CharField(max_length=200, blank=True)
    equipamento_sn = models.CharField(max_length=100, blank=True)
    mensagem = models.TextField()
    nivel = models.CharField(max_length=10, db_index=True)
    inicio = models.DateTimeField()
    fim = models.DateTimeField(null=True, blank=True)
    estado = models.CharField(max_length=15, db_index=True)
    sugestao = models.TextField(blank=True)
    anotacoes = models.TextField(blank=True)
    notificacao_enviada = models.BooleanField(default=False)
    payload_bruto = models.JSONField()  # NUNCA incluir em serializers
    criado_em = models.DateTimeField(auto_now_add=True)
    atualizado_em = models.DateTimeField(auto_now=True)
```

From backend_monitoramento/api/pagination.py (Plan 01):
```python
class PaginacaoSnapshots(PageNumberPagination):
    page_size = 100
```

From backend_monitoramento/api/urls.py (apos Plan 02 — sera atualizado):
```python
router = DefaultRouter()
router.register('usinas', UsinaViewSet, basename='usina')
# ... urlpatterns com auth, ping, garantias, router.urls
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serializers, filters, views e testes de Inversores</name>
  <files>
    backend_monitoramento/api/serializers/inversores.py,
    backend_monitoramento/api/filters/inversores.py,
    backend_monitoramento/api/views/inversores.py,
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/tests/test_inversores.py
  </files>
  <read_first>
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/pagination.py,
    backend_monitoramento/api/views/usinas.py,
    backend_monitoramento/api/serializers/usinas.py,
    backend_monitoramento/api/tests/test_inversores.py,
    backend_monitoramento/api/tests/conftest.py
  </read_first>
  <behavior>
    - GET /api/inversores/ sem token retorna 401
    - GET /api/inversores/ autenticado retorna lista paginada
    - GET /api/inversores/?usina={id} retorna apenas inversores daquela usina
    - GET /api/inversores/?modelo=Solis retorna inversores com modelo contendo "Solis"
    - GET /api/inversores/{id}/ retorna detalhe com ultimo_snapshot completo (pac_kw, tensao_ac_v, corrente_ac_a, tensao_dc_v, corrente_dc_a, frequencia_hz, temperatura_c)
    - GET /api/inversores/{id}/ sem snapshot retorna ultimo_snapshot=null
    - GET /api/inversores/{id}/snapshots/ retorna historico paginado com page_size=100
    - POST e DELETE retornam 405
    - payload_bruto NUNCA aparece em nenhuma resposta
  </behavior>
  <action>
1. Criar `backend_monitoramento/api/serializers/inversores.py`:

```python
from rest_framework import serializers
from usinas.models import Inversor, SnapshotInversor


class SnapshotInversorSerializer(serializers.ModelSerializer):
    """Serializer de SnapshotInversor — EXCLUI payload_bruto (T-2-03)."""
    class Meta:
        model = SnapshotInversor
        fields = [
            'id', 'coletado_em', 'data_medicao', 'estado',
            'pac_kw', 'energia_hoje_kwh', 'energia_total_kwh',
            'soc_bateria', 'strings_mppt',
            'tensao_ac_v', 'corrente_ac_a',
            'tensao_dc_v', 'corrente_dc_a',
            'frequencia_hz', 'temperatura_c',
        ]
        # payload_bruto EXCLUIDO


class InversorListSerializer(serializers.ModelSerializer):
    """Serializer de listagem de inversores."""
    usina_nome = serializers.CharField(source='usina.nome', read_only=True)

    class Meta:
        model = Inversor
        fields = [
            'id', 'usina', 'usina_nome', 'id_inversor_provedor',
            'numero_serie', 'modelo', 'criado_em',
        ]


class InversorDetalheSerializer(serializers.ModelSerializer):
    """Serializer de detalhe de inversor (INV-02: inclui ultimo snapshot completo)."""
    usina_nome = serializers.CharField(source='usina.nome', read_only=True)
    ultimo_snapshot = SnapshotInversorSerializer(read_only=True)

    class Meta:
        model = Inversor
        fields = [
            'id', 'usina', 'usina_nome', 'id_inversor_provedor',
            'numero_serie', 'modelo', 'ultimo_snapshot', 'criado_em',
        ]
```

2. Criar `backend_monitoramento/api/filters/inversores.py`:

```python
import django_filters
from usinas.models import Inversor


class InversorFilterSet(django_filters.FilterSet):
    """Filtros para listagem de inversores (D-03)."""
    usina = django_filters.UUIDFilter(field_name='usina__id')
    provedor = django_filters.CharFilter(field_name='usina__provedor', lookup_expr='exact')
    modelo = django_filters.CharFilter(field_name='modelo', lookup_expr='icontains')

    class Meta:
        model = Inversor
        fields = ['usina', 'provedor', 'modelo']
```

3. Criar `backend_monitoramento/api/views/inversores.py`:

```python
from rest_framework import viewsets
from rest_framework.decorators import action
from rest_framework.response import Response

from usinas.models import Inversor
from api.serializers.inversores import (
    InversorListSerializer,
    InversorDetalheSerializer,
    SnapshotInversorSerializer,
)
from api.filters.inversores import InversorFilterSet
from api.pagination import PaginacaoSnapshots


class InversorViewSet(viewsets.ModelViewSet):
    """
    ViewSet de Inversores — read-only + snapshots.
    POST, PUT, DELETE desabilitados — inversores sao criados pela coleta.
    """
    http_method_names = ['get', 'head', 'options']
    filterset_class = InversorFilterSet

    def get_queryset(self):
        return Inversor.objects.select_related(
            'usina', 'ultimo_snapshot'
        ).order_by('usina__nome', 'numero_serie')

    def get_serializer_class(self):
        if self.action == 'list':
            return InversorListSerializer
        return InversorDetalheSerializer

    @action(detail=True, methods=['get'], url_path='snapshots')
    def snapshots(self, request, pk=None):
        """INV-03: Historico paginado de snapshots do inversor."""
        inversor = self.get_object()
        qs = inversor.snapshots.order_by('-coletado_em')
        paginator = PaginacaoSnapshots()
        page = paginator.paginate_queryset(qs, request)
        serializer = SnapshotInversorSerializer(page, many=True)
        return paginator.get_paginated_response(serializer.data)
```

4. Atualizar `backend_monitoramento/api/urls.py` — adicionar `InversorViewSet` ao router:
   - Adicionar import: `from .views.inversores import InversorViewSet`
   - Adicionar registro: `router.register('inversores', InversorViewSet, basename='inversor')`

5. Remover `pytest.mark.skip` dos testes em `test_inversores.py` e implementar corpos:
   - Lista: verificar 200, campos presentes (usina_nome, numero_serie, modelo)
   - Filtro por usina: `?usina={usina.id}` retorna apenas inversores daquela usina
   - Filtro por modelo: `?modelo=Solis` retorna inversores com modelo contendo "Solis"
   - Detalhe com snapshot: verificar campos pac_kw, tensao_ac_v, corrente_ac_a, frequencia_hz, temperatura_c
   - Detalhe sem snapshot: verificar ultimo_snapshot == None
   - Snapshots: verificar paginacao
   - 401 sem token
   - CRITICO: verificar que 'payload_bruto' NAO aparece em respostas de snapshot
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && pytest api/tests/test_inversores.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `api/serializers/inversores.py` contem SnapshotInversorSerializer, InversorListSerializer, InversorDetalheSerializer
    - `api/serializers/inversores.py` NAO contem 'payload_bruto' em nenhum fields
    - `api/filters/inversores.py` contem InversorFilterSet com campos usina, provedor, modelo
    - `api/views/inversores.py` contem InversorViewSet com http_method_names = ['get', 'head', 'options']
    - `api/urls.py` contem `router.register('inversores', InversorViewSet`
    - `pytest api/tests/test_inversores.py -x` retorna exit 0
    - `pytest api/tests/test_usinas.py api/tests/test_garantias.py -x` continua passando
  </acceptance_criteria>
  <done>InversorViewSet completo com list, detail, snapshots; serializers com exclusao de payload_bruto; filtros por usina, provedor, modelo; todos os testes de INV-01..03 passando</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serializers, filters, views e testes de Alertas</name>
  <files>
    backend_monitoramento/api/serializers/alertas.py,
    backend_monitoramento/api/filters/alertas.py,
    backend_monitoramento/api/views/alertas.py,
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/tests/test_alertas.py
  </files>
  <read_first>
    backend_monitoramento/alertas/models.py,
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/views/usinas.py,
    backend_monitoramento/api/tests/test_alertas.py,
    backend_monitoramento/api/tests/conftest.py
  </read_first>
  <behavior>
    - GET /api/alertas/ sem token retorna 401
    - GET /api/alertas/ autenticado retorna lista paginada com campo com_garantia
    - GET /api/alertas/?estado=ativo retorna apenas alertas ativos
    - GET /api/alertas/?nivel=aviso retorna apenas alertas nivel aviso
    - GET /api/alertas/?usina={id} retorna apenas alertas daquela usina
    - GET /api/alertas/{id}/ retorna detalhe com com_garantia
    - com_garantia=true quando usina tem garantia ativa; false caso contrario
    - PATCH /api/alertas/{id}/ com {estado: 'em_atendimento'} atualiza estado
    - PATCH /api/alertas/{id}/ com {anotacoes: 'texto'} atualiza anotacoes
    - POST /api/alertas/ retorna 405
    - DELETE /api/alertas/{id}/ retorna 405
    - payload_bruto NUNCA aparece em nenhuma resposta
  </behavior>
  <action>
1. Criar `backend_monitoramento/api/serializers/alertas.py`:

```python
from rest_framework import serializers
from alertas.models import Alerta
from usinas.models import GarantiaUsina


class AlertaListSerializer(serializers.ModelSerializer):
    """Serializer de listagem de alertas (ALT-04: inclui com_garantia)."""
    com_garantia = serializers.SerializerMethodField()
    usina_nome = serializers.CharField(source='usina.nome', read_only=True)

    class Meta:
        model = Alerta
        fields = [
            'id', 'usina', 'usina_nome', 'mensagem', 'nivel',
            'estado', 'inicio', 'fim', 'com_garantia',
            'criado_em', 'atualizado_em',
        ]
        # payload_bruto EXCLUIDO

    def get_com_garantia(self, obj) -> bool:
        """
        ALT-04: indica se usina do alerta tem garantia ativa no momento.
        Usa select_related('usina__garantia') para evitar N+1.
        """
        try:
            garantia = obj.usina.garantia
            return garantia.ativa
        except GarantiaUsina.DoesNotExist:
            return False


class AlertaDetalheSerializer(serializers.ModelSerializer):
    """Serializer de detalhe de alerta (ALT-02 + ALT-04)."""
    com_garantia = serializers.SerializerMethodField()
    usina_nome = serializers.CharField(source='usina.nome', read_only=True)

    class Meta:
        model = Alerta
        fields = [
            'id', 'usina', 'usina_nome',
            'catalogo_alarme', 'id_alerta_provedor', 'equipamento_sn',
            'mensagem', 'nivel', 'estado', 'inicio', 'fim',
            'sugestao', 'anotacoes', 'com_garantia',
            'criado_em', 'atualizado_em',
        ]
        # payload_bruto EXCLUIDO — notificacao_enviada e campo interno

    def get_com_garantia(self, obj) -> bool:
        try:
            garantia = obj.usina.garantia
            return garantia.ativa
        except GarantiaUsina.DoesNotExist:
            return False


class AlertaPatchSerializer(serializers.ModelSerializer):
    """Serializer de escrita para PATCH /api/alertas/{id}/ — apenas estado e anotacoes (ALT-03)."""
    class Meta:
        model = Alerta
        fields = ['estado', 'anotacoes']
```

2. Criar `backend_monitoramento/api/filters/alertas.py`:

```python
import django_filters
from alertas.models import Alerta


class AlertaFilterSet(django_filters.FilterSet):
    """Filtros para listagem de alertas (D-03)."""
    estado = django_filters.CharFilter(field_name='estado', lookup_expr='exact')
    nivel = django_filters.CharFilter(field_name='nivel', lookup_expr='exact')
    usina = django_filters.UUIDFilter(field_name='usina__id')

    class Meta:
        model = Alerta
        fields = ['estado', 'nivel', 'usina']
```

3. Criar `backend_monitoramento/api/views/alertas.py`:

```python
from rest_framework import viewsets

from alertas.models import Alerta
from api.serializers.alertas import (
    AlertaListSerializer,
    AlertaDetalheSerializer,
    AlertaPatchSerializer,
)
from api.filters.alertas import AlertaFilterSet


class AlertaViewSet(viewsets.ModelViewSet):
    """
    ViewSet de Alertas.
    GET list/detail, PATCH para estado/anotacoes.
    POST e DELETE desabilitados — alertas sao criados pela coleta.
    """
    http_method_names = ['get', 'patch', 'head', 'options']
    filterset_class = AlertaFilterSet

    def get_queryset(self):
        return Alerta.objects.select_related(
            'usina', 'usina__garantia', 'catalogo_alarme'
        ).order_by('-inicio')

    def get_serializer_class(self):
        if self.action == 'list':
            return AlertaListSerializer
        if self.action == 'partial_update':
            return AlertaPatchSerializer
        return AlertaDetalheSerializer
```

NOTA: `select_related('usina__garantia')` faz join com GarantiaUsina — evita N+1 para com_garantia. Quando a usina nao tem garantia, o acesso `obj.usina.garantia` lanca DoesNotExist, capturado no SerializerMethodField.

4. Atualizar `backend_monitoramento/api/urls.py` — adicionar `AlertaViewSet` ao router:
   - Adicionar import: `from .views.alertas import AlertaViewSet`
   - Adicionar registro: `router.register('alertas', AlertaViewSet, basename='alerta')`

5. Remover `pytest.mark.skip` dos testes em `test_alertas.py` e implementar corpos:
   - Lista: verificar 200, campo com_garantia presente em cada resultado
   - Filtro por estado: `?estado=ativo` retorna apenas alertas ativos
   - Filtro por nivel: `?nivel=aviso` retorna apenas nivel aviso
   - Filtro por usina: `?usina={usina.id}`
   - com_garantia=true: criar alerta + garantia_ativa, verificar com_garantia==True
   - com_garantia=false: criar alerta sem garantia, verificar com_garantia==False
   - Detalhe: verificar campos completos (mensagem, nivel, estado, sugestao, anotacoes, com_garantia)
   - PATCH estado: `client.patch(f'/api/alertas/{alerta.id}/', {'estado': 'em_atendimento'}, ...)` verificar 200 e alerta atualizado
   - PATCH anotacoes: verificar anotacoes persistiu
   - POST 405 e DELETE 405
   - 401 sem token
   - CRITICO: verificar que 'payload_bruto' NAO aparece em nenhuma resposta
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && pytest api/tests/test_alertas.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `api/serializers/alertas.py` contem AlertaListSerializer, AlertaDetalheSerializer, AlertaPatchSerializer
    - `api/serializers/alertas.py` NAO contem 'payload_bruto' em nenhum fields
    - `api/serializers/alertas.py` AlertaPatchSerializer.Meta.fields == ['estado', 'anotacoes']
    - `api/filters/alertas.py` contem AlertaFilterSet com campos estado, nivel, usina
    - `api/views/alertas.py` contem AlertaViewSet com http_method_names = ['get', 'patch', 'head', 'options']
    - `api/views/alertas.py` queryset usa select_related('usina', 'usina__garantia')
    - `api/urls.py` contem `router.register('alertas', AlertaViewSet`
    - `pytest api/tests/test_alertas.py -x` retorna exit 0
    - `pytest api/tests/ -x --tb=short` completa sem falhas (suite inteira)
  </acceptance_criteria>
  <done>AlertaViewSet completo com list, detail, patch; com_garantia calculado via select_related sem N+1; filtros por estado, nivel, usina; payload_bruto excluido; todos os testes de ALT-01..04 passando</done>
</task>

</tasks>

<threat_model>
  <threat id="T-2-07" severity="high">
    <description>Exposicao de payload_bruto em serializers de SnapshotInversor e Alerta — contem dados brutos do provedor</description>
    <mitigation>SnapshotInversorSerializer, AlertaListSerializer e AlertaDetalheSerializer declaram fields explicitamente SEM payload_bruto. Testes verificam ausencia em todas as respostas.</mitigation>
  </threat>
  <threat id="T-2-08" severity="high">
    <description>PATCH de campos nao permitidos em alerta (ex: nivel, usina, payload_bruto) via mass assignment</description>
    <mitigation>AlertaPatchSerializer declara fields = ['estado', 'anotacoes'] — APENAS esses dois campos aceitos. Campos extras ignorados pelo DRF.</mitigation>
  </threat>
  <threat id="T-2-09" severity="medium">
    <description>POST/DELETE expostos por acidente em InversorViewSet e AlertaViewSet</description>
    <mitigation>InversorViewSet: http_method_names = ['get', 'head', 'options']. AlertaViewSet: http_method_names = ['get', 'patch', 'head', 'options']. Testes verificam 405 para POST e DELETE.</mitigation>
  </threat>
  <threat id="T-2-10" severity="medium">
    <description>N+1 em com_garantia — acesso a usina.garantia para cada alerta na listagem</description>
    <mitigation>get_queryset() usa select_related('usina', 'usina__garantia') — um unico JOIN resolve o relacionamento. Volume controlado (PAGE_SIZE=20).</mitigation>
  </threat>
</threat_model>

<verification>
1. `pytest api/tests/test_inversores.py -x --tb=short` — todos passam
2. `pytest api/tests/test_alertas.py -x --tb=short` — todos passam
3. `pytest api/tests/ -x --tb=short` — suite inteira verde
4. Nenhum serializer contem 'payload_bruto' nos fields
5. InversorViewSet nao permite POST/PUT/DELETE; AlertaViewSet nao permite POST/DELETE
</verification>

<success_criteria>
- GET /api/inversores/ retorna lista paginada com filtros por usina, provedor, modelo
- GET /api/inversores/{id}/ retorna detalhe com ultimo snapshot completo
- GET /api/inversores/{id}/snapshots/ retorna historico com page_size=100
- GET /api/alertas/ retorna lista com filtros por estado, nivel, usina e campo com_garantia
- GET /api/alertas/{id}/ retorna detalhe completo com com_garantia
- PATCH /api/alertas/{id}/ atualiza estado e anotacoes; POST/DELETE retornam 405
- com_garantia reflete situacao real da garantia no momento da requisicao
- payload_bruto nunca aparece em respostas
- Todos os endpoints retornam 401 sem token
- Suite inteira de testes verde
</success_criteria>

<output>
After completion, create `.planning/phases/02-rest-endpoints/02-03-SUMMARY.md`
</output>
