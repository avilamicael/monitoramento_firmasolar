---
phase: 02-rest-endpoints
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend_monitoramento/api/serializers/usinas.py
  - backend_monitoramento/api/serializers/garantias.py
  - backend_monitoramento/api/filters/usinas.py
  - backend_monitoramento/api/views/usinas.py
  - backend_monitoramento/api/views/garantias.py
  - backend_monitoramento/api/urls.py
  - backend_monitoramento/api/tests/test_usinas.py
  - backend_monitoramento/api/tests/test_garantias.py
autonomous: true
requirements:
  - USN-01
  - USN-02
  - USN-03
  - USN-04
  - USN-05
  - GAR-02
  - GAR-03
  - GAR-04
  - GAR-05
  - GAR-06

must_haves:
  truths:
    - "Administrador lista usinas filtradas por provedor, ativo e status_garantia"
    - "Detalhe de usina retorna inversores e ultimo snapshot"
    - "PATCH atualiza nome e capacidade; POST e DELETE retornam 405"
    - "Historico de snapshots de usina paginado com page_size=100"
    - "PUT /api/usinas/{id}/garantia/ cria ou substitui garantia com resposta imediata incluindo data_fim e dias_restantes"
    - "GET /api/garantias/ filtra por ativas, vencendo, vencidas via parametro filtro"
    - "status_garantia retorna exatamente 'ativa', 'vencida' ou 'sem_garantia'"
    - "Todos os endpoints retornam 401 sem token"
  artifacts:
    - path: "backend_monitoramento/api/serializers/usinas.py"
      provides: "UsinaListSerializer, UsinaDetalheSerializer, UsinaPatchSerializer, SnapshotUsinaSerializer"
      min_lines: 50
    - path: "backend_monitoramento/api/serializers/garantias.py"
      provides: "GarantiaUsinaSerializer, GarantiaUsinaEscritaSerializer"
      min_lines: 20
    - path: "backend_monitoramento/api/filters/usinas.py"
      provides: "UsinaFilterSet com filtro status_garantia customizado"
      contains: "filtrar_status_garantia"
    - path: "backend_monitoramento/api/views/usinas.py"
      provides: "UsinaViewSet com actions snapshots e garantia"
      contains: "class UsinaViewSet"
    - path: "backend_monitoramento/api/views/garantias.py"
      provides: "GarantiaListView com filtro por parametro filtro"
      contains: "class GarantiaListView"
  key_links:
    - from: "backend_monitoramento/api/views/usinas.py"
      to: "backend_monitoramento/api/serializers/usinas.py"
      via: "serializer_class"
      pattern: "UsinaListSerializer|UsinaDetalheSerializer"
    - from: "backend_monitoramento/api/views/usinas.py"
      to: "backend_monitoramento/api/filters/usinas.py"
      via: "filterset_class"
      pattern: "UsinaFilterSet"
    - from: "backend_monitoramento/api/urls.py"
      to: "backend_monitoramento/api/views/usinas.py"
      via: "router.register"
      pattern: "router.register.*usinas.*UsinaViewSet"
---

<objective>
Implementar os endpoints de Usinas (list, detail, patch, snapshots) e Garantias (upsert por usina, listagem com filtros) com serializers, filters, views, URL wiring e testes completos.

Purpose: Entregar USN-01..05 e GAR-02..06 — o administrador consegue listar, detalhar, editar usinas e gerir garantias via API.
Output: ViewSets, serializers, filtros e testes verdes para usinas e garantias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rest-endpoints/02-CONTEXT.md
@.planning/phases/02-rest-endpoints/02-RESEARCH.md
@.planning/phases/02-rest-endpoints/02-01-SUMMARY.md

<interfaces>
<!-- Models — contratos que serializers devem respeitar -->

From backend_monitoramento/usinas/models.py:
```python
class Usina(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4)
    id_usina_provedor = models.CharField(max_length=200)
    provedor = models.CharField(max_length=30)
    credencial = models.ForeignKey('provedores.CredencialProvedor', on_delete=models.PROTECT, related_name='usinas')
    nome = models.CharField(max_length=200)
    capacidade_kwp = models.FloatField(default=0.0)
    fuso_horario = models.CharField(max_length=50, default='America/Sao_Paulo')
    endereco = models.TextField(blank=True)
    ativo = models.BooleanField(default=True)
    ultimo_snapshot = models.OneToOneField('SnapshotUsina', null=True, blank=True, related_name='+')
    criado_em = models.DateTimeField(auto_now_add=True)
    atualizado_em = models.DateTimeField(auto_now=True)

class SnapshotUsina(models.Model):
    id = models.UUIDField(primary_key=True)
    usina = models.ForeignKey(Usina, related_name='snapshots')
    coletado_em = models.DateTimeField()
    potencia_kw = models.FloatField()
    energia_hoje_kwh = models.FloatField()
    energia_mes_kwh = models.FloatField()
    energia_total_kwh = models.FloatField()
    status = models.CharField(max_length=15)
    qtd_inversores = models.IntegerField()
    qtd_inversores_online = models.IntegerField()
    qtd_alertas = models.IntegerField()
    payload_bruto = models.JSONField()  # NUNCA incluir em serializers

class Inversor(models.Model):
    id = models.UUIDField(primary_key=True)
    usina = models.ForeignKey(Usina, related_name='inversores')
    id_inversor_provedor = models.CharField(max_length=200)
    numero_serie = models.CharField(max_length=100, blank=True)
    modelo = models.CharField(max_length=100, blank=True)
    ultimo_snapshot = models.OneToOneField('SnapshotInversor', null=True, blank=True, related_name='+')

class GarantiaUsina(models.Model):
    usina = models.OneToOneField(Usina, related_name='garantia')
    data_inicio = models.DateField()
    meses = models.PositiveIntegerField()
    observacoes = models.TextField(blank=True)
    # Properties (NAO sao colunas — NAO podem ser usadas em ORM filter):
    # data_fim -> data_inicio + relativedelta(months=meses)
    # ativa -> data_fim >= today
    # dias_restantes -> max((data_fim - today).days, 0)
```

From backend_monitoramento/api/pagination.py (criado no Plan 01):
```python
class PaginacaoSnapshots(PageNumberPagination):
    page_size = 100
    page_size_query_param = 'page_size'
    max_page_size = 500
```

From backend_monitoramento/api/urls.py (existente — sera modificado):
```python
urlpatterns = [
    path('auth/token/', TokenObtainPairView.as_view(), name='token_obtain_pair'),
    path('auth/token/refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    path('ping/', PingView.as_view(), name='api_ping'),
]
```

From backend_monitoramento/config/settings/base.py:
- REST_FRAMEWORK.PAGE_SIZE = 20
- REST_FRAMEWORK.DEFAULT_FILTER_BACKENDS = ['django_filters.rest_framework.DjangoFilterBackend']
- REST_FRAMEWORK.DEFAULT_PERMISSION_CLASSES = ['rest_framework.permissions.IsAuthenticated']
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serializers, filters, views e URLs de Usinas</name>
  <files>
    backend_monitoramento/api/serializers/usinas.py,
    backend_monitoramento/api/filters/usinas.py,
    backend_monitoramento/api/views/usinas.py,
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/tests/test_usinas.py
  </files>
  <read_first>
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/urls.py,
    backend_monitoramento/api/pagination.py,
    backend_monitoramento/api/tests/test_usinas.py,
    backend_monitoramento/api/tests/conftest.py,
    backend_monitoramento/api/views/__init__.py
  </read_first>
  <behavior>
    - GET /api/usinas/ sem token retorna 401
    - GET /api/usinas/ autenticado retorna lista paginada com campo status_garantia
    - GET /api/usinas/?provedor=solis retorna apenas usinas solis
    - GET /api/usinas/?ativo=true retorna apenas usinas ativas
    - GET /api/usinas/?status_garantia=ativa retorna usinas com garantia ativa
    - GET /api/usinas/?status_garantia=sem_garantia retorna usinas sem GarantiaUsina
    - GET /api/usinas/?status_garantia=vencida retorna usinas com garantia vencida
    - GET /api/usinas/{id}/ retorna detalhe com inversores[], ultimo_snapshot, status_garantia
    - GET /api/usinas/{id}/ para usina sem snapshot retorna ultimo_snapshot=null
    - PATCH /api/usinas/{id}/ com {nome: "Novo"} atualiza nome
    - PATCH /api/usinas/{id}/ com {capacidade_kwp: 15.0} atualiza capacidade
    - POST /api/usinas/ retorna 405
    - DELETE /api/usinas/{id}/ retorna 405
    - GET /api/usinas/{id}/snapshots/ retorna historico paginado com page_size=100
    - payload_bruto NUNCA aparece em nenhuma resposta de snapshot
  </behavior>
  <action>
1. Criar `backend_monitoramento/api/serializers/usinas.py`:

```python
from rest_framework import serializers
from usinas.models import Usina, SnapshotUsina, GarantiaUsina


class SnapshotUsinaSerializer(serializers.ModelSerializer):
    """Serializer de SnapshotUsina — EXCLUI payload_bruto (T-2-03)."""
    class Meta:
        model = SnapshotUsina
        fields = [
            'id', 'coletado_em', 'data_medicao', 'potencia_kw',
            'energia_hoje_kwh', 'energia_mes_kwh', 'energia_total_kwh',
            'status', 'qtd_inversores', 'qtd_inversores_online', 'qtd_alertas',
        ]
        # payload_bruto EXCLUIDO — dados brutos do provedor, potencialmente sensiveis


class InversorResumoSerializer(serializers.ModelSerializer):
    """Inversor resumido para uso dentro do detalhe de usina."""
    class Meta:
        from usinas.models import Inversor
        model = Inversor
        fields = ['id', 'numero_serie', 'modelo', 'id_inversor_provedor']


class UsinaListSerializer(serializers.ModelSerializer):
    """Serializer de listagem de usinas (D-04: inclui status_garantia)."""
    status_garantia = serializers.SerializerMethodField()

    class Meta:
        model = Usina
        fields = [
            'id', 'nome', 'provedor', 'capacidade_kwp', 'ativo',
            'status_garantia', 'criado_em', 'atualizado_em',
        ]

    def get_status_garantia(self, obj) -> str:
        """D-04: 3 valores — 'ativa', 'vencida', 'sem_garantia'."""
        try:
            garantia = obj.garantia
        except GarantiaUsina.DoesNotExist:
            return 'sem_garantia'
        return 'ativa' if garantia.ativa else 'vencida'


class UsinaDetalheSerializer(serializers.ModelSerializer):
    """Serializer de detalhe de usina (USN-02: inclui inversores e ultimo_snapshot)."""
    status_garantia = serializers.SerializerMethodField()
    ultimo_snapshot = SnapshotUsinaSerializer(read_only=True)
    inversores = InversorResumoSerializer(many=True, read_only=True)

    class Meta:
        model = Usina
        fields = [
            'id', 'nome', 'provedor', 'capacidade_kwp', 'ativo',
            'fuso_horario', 'endereco', 'status_garantia',
            'ultimo_snapshot', 'inversores', 'criado_em', 'atualizado_em',
        ]

    def get_status_garantia(self, obj) -> str:
        try:
            garantia = obj.garantia
        except GarantiaUsina.DoesNotExist:
            return 'sem_garantia'
        return 'ativa' if garantia.ativa else 'vencida'


class UsinaPatchSerializer(serializers.ModelSerializer):
    """Serializer de escrita para PATCH /api/usinas/{id}/ — apenas nome e capacidade (USN-03)."""
    class Meta:
        model = Usina
        fields = ['nome', 'capacidade_kwp']
```

NOTA: A classe `InversorResumoSerializer` deve importar `Inversor` no escopo do modulo (no topo), nao dentro de `Meta`. Corrigir para:
```python
from usinas.models import Usina, SnapshotUsina, Inversor, GarantiaUsina

class InversorResumoSerializer(serializers.ModelSerializer):
    class Meta:
        model = Inversor
        fields = ['id', 'numero_serie', 'modelo', 'id_inversor_provedor']
```

2. Criar `backend_monitoramento/api/filters/usinas.py`:

```python
import datetime
import django_filters
from django.utils import timezone
from usinas.models import Usina, GarantiaUsina


class UsinaFilterSet(django_filters.FilterSet):
    """Filtros para listagem de usinas (D-03, D-04)."""
    provedor = django_filters.CharFilter(field_name='provedor', lookup_expr='exact')
    ativo = django_filters.BooleanFilter(field_name='ativo')
    status_garantia = django_filters.CharFilter(method='filtrar_status_garantia')

    class Meta:
        model = Usina
        fields = ['provedor', 'ativo']

    def filtrar_status_garantia(self, queryset, name, value):
        """
        Filtro customizado por status de garantia (D-04).
        CRITICO: data_fim e @property, NAO coluna SQL.
        Para 'sem_garantia': ORM puro (garantia__isnull=True).
        Para 'ativa'/'vencida': filtrar IDs em Python (volume controlado).
        """
        hoje = timezone.now().date()
        if value == 'sem_garantia':
            return queryset.filter(garantia__isnull=True)
        elif value == 'ativa':
            ids_ativas = [
                g.usina_id for g in GarantiaUsina.objects.select_related('usina').all()
                if g.data_fim >= hoje
            ]
            return queryset.filter(id__in=ids_ativas)
        elif value == 'vencida':
            ids_vencidas = [
                g.usina_id for g in GarantiaUsina.objects.select_related('usina').all()
                if g.data_fim < hoje
            ]
            return queryset.filter(id__in=ids_vencidas)
        return queryset
```

3. Criar `backend_monitoramento/api/views/usinas.py`:

```python
from rest_framework import viewsets
from rest_framework.decorators import action
from rest_framework.response import Response

from usinas.models import Usina, GarantiaUsina
from api.serializers.usinas import (
    UsinaListSerializer,
    UsinaDetalheSerializer,
    UsinaPatchSerializer,
    SnapshotUsinaSerializer,
)
from api.serializers.garantias import GarantiaUsinaSerializer, GarantiaUsinaEscritaSerializer
from api.filters.usinas import UsinaFilterSet
from api.pagination import PaginacaoSnapshots


class UsinaViewSet(viewsets.ModelViewSet):
    """
    ViewSet de Usinas.
    GET list/detail, PATCH para nome/capacidade.
    POST e DELETE desabilitados — usinas sao criadas pela coleta automatica.
    """
    http_method_names = ['get', 'patch', 'head', 'options']
    filterset_class = UsinaFilterSet

    def get_queryset(self):
        return Usina.objects.select_related(
            'ultimo_snapshot', 'garantia'
        ).prefetch_related('inversores').order_by('nome')

    def get_serializer_class(self):
        if self.action == 'list':
            return UsinaListSerializer
        if self.action == 'partial_update':
            return UsinaPatchSerializer
        return UsinaDetalheSerializer

    @action(detail=True, methods=['get'], url_path='snapshots')
    def snapshots(self, request, pk=None):
        """USN-05: Historico paginado de snapshots da usina."""
        usina = self.get_object()
        qs = usina.snapshots.order_by('-coletado_em')
        paginator = PaginacaoSnapshots()
        page = paginator.paginate_queryset(qs, request)
        serializer = SnapshotUsinaSerializer(page, many=True)
        return paginator.get_paginated_response(serializer.data)

    @action(detail=True, methods=['put'], url_path='garantia')
    def garantia(self, request, pk=None):
        """GAR-02: Upsert de garantia da usina via PUT."""
        usina = self.get_object()
        serializer = GarantiaUsinaEscritaSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        garantia, _ = GarantiaUsina.objects.update_or_create(
            usina=usina,
            defaults=serializer.validated_data,
        )
        return Response(GarantiaUsinaSerializer(garantia).data)
```

4. Atualizar `backend_monitoramento/api/urls.py` para incluir router e ViewSets:

```python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView

from .views import PingView
from .views.usinas import UsinaViewSet
from .views.garantias import GarantiaListView

router = DefaultRouter()
router.register('usinas', UsinaViewSet, basename='usina')

urlpatterns = [
    path('auth/token/', TokenObtainPairView.as_view(), name='token_obtain_pair'),
    path('auth/token/refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    path('ping/', PingView.as_view(), name='api_ping'),
    path('garantias/', GarantiaListView.as_view(), name='garantia-list'),
    path('', include(router.urls)),
]
```

5. Remover os `pytest.mark.skip` dos testes em `test_usinas.py` e implementar os corpos dos testes usando as fixtures do conftest. Cada teste deve:
   - Usar `client.get(reverse('usina-list'), HTTP_AUTHORIZATION=f"Bearer {tokens['access']}")` para autenticacao
   - Verificar status codes (200 para sucesso, 401 sem token, 405 para metodos proibidos)
   - Verificar campos presentes na resposta (`status_garantia`, `inversores`, `ultimo_snapshot`)
   - Verificar valores de filtros (provedor, status_garantia)
   - Para PATCH: usar `client.patch(reverse('usina-detail', args=[usina.id]), data, content_type='application/json', HTTP_AUTHORIZATION=...)`
   - Para snapshots: verificar paginacao com `response.json()['results']` e `response.json()['count']`
   - CRITICO: verificar que `payload_bruto` NAO aparece em nenhuma resposta de snapshot

Manter testes existentes da Phase 1 (test_auth.py, test_cors.py) intactos.
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && pytest api/tests/test_usinas.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `api/serializers/usinas.py` contem classes UsinaListSerializer, UsinaDetalheSerializer, UsinaPatchSerializer, SnapshotUsinaSerializer
    - `api/serializers/usinas.py` NAO contem a string `payload_bruto` em nenhum campo fields
    - `api/filters/usinas.py` contem class UsinaFilterSet com metodo filtrar_status_garantia
    - `api/views/usinas.py` contem class UsinaViewSet com http_method_names = ['get', 'patch', 'head', 'options']
    - `api/views/usinas.py` contem decorador @action para snapshots e garantia
    - `api/urls.py` contem `router.register('usinas', UsinaViewSet`
    - `api/urls.py` contem `path('garantias/', GarantiaListView`
    - `pytest api/tests/test_usinas.py -x` retorna exit 0 (todos passam)
    - Testes existentes continuam passando: `pytest api/tests/test_auth.py -x` retorna exit 0
  </acceptance_criteria>
  <done>UsinaViewSet completo com list, detail, patch, snapshots e garantia; serializers com exclusao de payload_bruto; filtros por provedor, ativo, status_garantia; todos os testes de USN-01..05 passando</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serializers, views e testes de Garantias</name>
  <files>
    backend_monitoramento/api/serializers/garantias.py,
    backend_monitoramento/api/views/garantias.py,
    backend_monitoramento/api/tests/test_garantias.py
  </files>
  <read_first>
    backend_monitoramento/usinas/models.py,
    backend_monitoramento/api/views/usinas.py,
    backend_monitoramento/api/serializers/usinas.py,
    backend_monitoramento/api/tests/test_garantias.py,
    backend_monitoramento/api/tests/conftest.py
  </read_first>
  <behavior>
    - PUT /api/usinas/{id}/garantia/ cria garantia com data_inicio e meses, resposta inclui data_fim e dias_restantes
    - PUT /api/usinas/{id}/garantia/ com garantia existente substitui dados
    - PUT /api/usinas/{id}/garantia/ sem token retorna 401
    - GET /api/garantias/ sem filtro retorna todas as garantias
    - GET /api/garantias/?filtro=ativas retorna apenas garantias ativas
    - GET /api/garantias/?filtro=vencidas retorna apenas vencidas
    - GET /api/garantias/?filtro=vencendo retorna garantias vencendo em 30 dias
    - Cada garantia na resposta inclui dias_restantes como inteiro
    - GET /api/garantias/ sem token retorna 401
  </behavior>
  <action>
1. Criar `backend_monitoramento/api/serializers/garantias.py`:

```python
from rest_framework import serializers
from usinas.models import GarantiaUsina


class GarantiaUsinaSerializer(serializers.ModelSerializer):
    """Serializer de leitura de GarantiaUsina (GAR-04: inclui data_fim e dias_restantes)."""
    data_fim = serializers.DateField(read_only=True, source='data_fim')
    dias_restantes = serializers.IntegerField(read_only=True, source='dias_restantes')
    ativa = serializers.BooleanField(read_only=True, source='ativa')
    usina_id = serializers.UUIDField(source='usina.id', read_only=True)
    usina_nome = serializers.CharField(source='usina.nome', read_only=True)

    class Meta:
        model = GarantiaUsina
        fields = [
            'id', 'usina_id', 'usina_nome',
            'data_inicio', 'meses', 'observacoes',
            'data_fim', 'dias_restantes', 'ativa',
            'criado_em', 'atualizado_em',
        ]


class GarantiaUsinaEscritaSerializer(serializers.Serializer):
    """Serializer de escrita para PUT /api/usinas/{id}/garantia/ (GAR-02)."""
    data_inicio = serializers.DateField()
    meses = serializers.IntegerField(min_value=1)
    observacoes = serializers.CharField(required=False, default='', allow_blank=True)
```

NOTA: `data_fim`, `dias_restantes` e `ativa` sao @property no model. O DRF `ModelSerializer` com `source='data_fim'` pode gerar conflito pois nao sao campos reais. Alternativa correta: usar `SerializerMethodField`:

```python
class GarantiaUsinaSerializer(serializers.ModelSerializer):
    data_fim = serializers.SerializerMethodField()
    dias_restantes = serializers.SerializerMethodField()
    ativa = serializers.SerializerMethodField()
    usina_id = serializers.UUIDField(source='usina.id', read_only=True)
    usina_nome = serializers.CharField(source='usina.nome', read_only=True)

    class Meta:
        model = GarantiaUsina
        fields = [
            'id', 'usina_id', 'usina_nome',
            'data_inicio', 'meses', 'observacoes',
            'data_fim', 'dias_restantes', 'ativa',
            'criado_em', 'atualizado_em',
        ]

    def get_data_fim(self, obj):
        return obj.data_fim.isoformat()

    def get_dias_restantes(self, obj) -> int:
        return obj.dias_restantes

    def get_ativa(self, obj) -> bool:
        return obj.ativa
```

2. Criar `backend_monitoramento/api/views/garantias.py`:

```python
from rest_framework import generics
from django.utils import timezone

from usinas.models import GarantiaUsina
from api.serializers.garantias import GarantiaUsinaSerializer


class GarantiaListView(generics.ListAPIView):
    """
    GET /api/garantias/ — lista todas as garantias (D-05).
    Filtro via query param ?filtro=ativas|vencendo|vencidas.
    CRITICO: data_fim e @property — NAO filtrar via ORM.
    Filtro em Python apos carregar todos os registros (volume controlado).
    """
    serializer_class = GarantiaUsinaSerializer

    def get_queryset(self):
        qs = GarantiaUsina.objects.select_related('usina').order_by('usina__nome')
        filtro = self.request.query_params.get('filtro')
        if not filtro:
            return qs

        hoje = timezone.now().date()
        todas = list(qs)

        if filtro == 'ativas':
            ids = [g.id for g in todas if g.data_fim >= hoje]
        elif filtro == 'vencidas':
            ids = [g.id for g in todas if g.data_fim < hoje]
        elif filtro == 'vencendo':
            import datetime
            limite = hoje + datetime.timedelta(days=30)
            ids = [g.id for g in todas if hoje <= g.data_fim <= limite]
        else:
            return qs

        return GarantiaUsina.objects.select_related('usina').filter(id__in=ids).order_by('usina__nome')
```

3. Remover os `pytest.mark.skip` dos testes em `test_garantias.py` e implementar os corpos. Testes devem:
   - PUT de garantia: `client.put(f'/api/usinas/{usina.id}/garantia/', {'data_inicio': '2025-01-01', 'meses': 24}, content_type='application/json', HTTP_AUTHORIZATION=...)`
   - Verificar resposta do PUT contem 'data_fim', 'dias_restantes', 'ativa'
   - PUT substituicao: criar garantia via fixture, PUT com novos dados, verificar que meses mudou
   - GET /api/garantias/ sem filtro: verificar count >= 2 (ativa + vencida)
   - GET /api/garantias/?filtro=ativas: verificar que todas as garantias retornadas tem ativa=true
   - GET /api/garantias/?filtro=vencidas: verificar que todas tem ativa=false
   - GET /api/garantias/?filtro=vencendo: criar fixture com garantia vencendo em 15 dias, verificar que aparece
   - Verificar dias_restantes e inteiro >= 0
   - 401 sem token
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/backend_monitoramento && pytest api/tests/test_garantias.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `api/serializers/garantias.py` contem classes GarantiaUsinaSerializer e GarantiaUsinaEscritaSerializer
    - `api/serializers/garantias.py` GarantiaUsinaSerializer inclui campos data_fim, dias_restantes, ativa
    - `api/views/garantias.py` contem class GarantiaListView com filtro via query param 'filtro'
    - `api/views/garantias.py` filtra garantias em Python (NAO usa .filter(data_fim__gte=...))
    - `pytest api/tests/test_garantias.py -x` retorna exit 0
    - `pytest api/tests/test_usinas.py -x` continua passando (nao quebrou nada)
  </acceptance_criteria>
  <done>GarantiaListView com filtros ativas/vencendo/vencidas via Python; GarantiaUsinaSerializer com data_fim/dias_restantes; upsert via UsinaViewSet @action; todos os testes de GAR-02..06 passando</done>
</task>

</tasks>

<threat_model>
  <threat id="T-2-03" severity="high">
    <description>Exposicao de payload_bruto em serializers de SnapshotUsina — contem dados brutos do provedor (API keys, tokens em metadata)</description>
    <mitigation>SnapshotUsinaSerializer declara fields explicitamente SEM payload_bruto. Teste verifica que 'payload_bruto' nao aparece em nenhuma resposta. Nenhum serializer usa fields='__all__'.</mitigation>
  </threat>
  <threat id="T-2-04" severity="high">
    <description>PATCH de campos nao permitidos em usina (ex: provedor, credencial, ativo) via mass assignment</description>
    <mitigation>UsinaPatchSerializer declara fields = ['nome', 'capacidade_kwp'] — APENAS esses dois campos aceitos. Qualquer campo extra no payload e ignorado pelo DRF.</mitigation>
  </threat>
  <threat id="T-2-05" severity="medium">
    <description>POST/DELETE expostos por acidente em UsinaViewSet (usinas criadas apenas pela coleta)</description>
    <mitigation>http_method_names = ['get', 'patch', 'head', 'options'] — POST e DELETE retornam 405. Teste explicito verifica 405 para ambos.</mitigation>
  </threat>
  <threat id="T-2-06" severity="medium">
    <description>Filtro status_garantia com valores inesperados poderia causar erro ou bypass</description>
    <mitigation>UsinaFilterSet.filtrar_status_garantia retorna queryset inalterado para valores nao reconhecidos (sem_garantia/ativa/vencida). Valores invalidos nao causam erro nem bypass.</mitigation>
  </threat>
</threat_model>

<verification>
1. `pytest api/tests/test_usinas.py -x --tb=short` — todos passam
2. `pytest api/tests/test_garantias.py -x --tb=short` — todos passam
3. `pytest api/tests/test_auth.py -x --tb=short` — Phase 1 continua verde
4. Nenhum serializer contem 'payload_bruto' nos fields
5. UsinaViewSet.http_method_names nao inclui 'post' nem 'delete'
</verification>

<success_criteria>
- GET /api/usinas/ retorna lista paginada com status_garantia
- Filtros por provedor, ativo, status_garantia funcionam
- GET /api/usinas/{id}/ retorna inversores + ultimo_snapshot
- PATCH /api/usinas/{id}/ atualiza nome e capacidade; POST/DELETE retornam 405
- GET /api/usinas/{id}/snapshots/ retorna historico com page_size=100
- PUT /api/usinas/{id}/garantia/ faz upsert e retorna data_fim + dias_restantes
- GET /api/garantias/?filtro=ativas/vencendo/vencidas funciona
- Todos os endpoints retornam 401 sem token
- payload_bruto nunca aparece em respostas
- Todos os testes de USN-01..05 e GAR-02..06 passam
</success_criteria>

<output>
After completion, create `.planning/phases/02-rest-endpoints/02-02-SUMMARY.md`
</output>
