---
phase: 05-usinas-garantias
plan: "03"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/admin/src/pages/GarantiasPage.tsx
  - frontend/admin/src/components/garantias/GarantiasTable.tsx
  - frontend/admin/src/components/garantias/GarantiaFormDialog.tsx
autonomous: false
requirements: [FE-09, FE-10, FE-11]

must_haves:
  truths:
    - "Administrador ve listagem de todas as garantias com status, data_fim e dias_restantes"
    - "Garantias com menos de 30 dias restantes exibem indicador visual vermelho"
    - "Administrador cria ou edita garantia via modal com preview de data_fim em tempo real"
    - "Apos salvar garantia, a listagem atualiza sem reload"
  artifacts:
    - path: "frontend/admin/src/pages/GarantiasPage.tsx"
      provides: "Pagina de listagem de garantias com filtros"
      min_lines: 30
    - path: "frontend/admin/src/components/garantias/GarantiasTable.tsx"
      provides: "Tabela com indicador vermelho para vencimento proximo"
      min_lines: 40
    - path: "frontend/admin/src/components/garantias/GarantiaFormDialog.tsx"
      provides: "Modal com form de garantia e preview de data_fim"
      min_lines: 50
  key_links:
    - from: "frontend/admin/src/pages/GarantiasPage.tsx"
      to: "useGarantias hook"
      via: "import useGarantias"
      pattern: "useGarantias"
    - from: "frontend/admin/src/components/garantias/GarantiaFormDialog.tsx"
      to: "/api/usinas/{id}/garantia/"
      via: "api.put"
      pattern: "api\\.put.*garantia"
---

<objective>
Implementar a pagina de garantias com listagem, indicador de vencimento proximo e modal de criacao/edicao de garantia.

Purpose: FE-09 (listagem garantias), FE-10 (indicador vermelho < 30 dias) e FE-11 (formulario criar/editar) completam a gestao de garantias.
Output: GarantiasPage funcional com tabela, indicadores visuais e modal de formulario com preview de data_fim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-usinas-garantias/05-CONTEXT.md
@.planning/phases/05-usinas-garantias/05-RESEARCH.md
@.planning/phases/05-usinas-garantias/05-01-SUMMARY.md

@frontend/admin/src/lib/api.ts
@frontend/admin/src/types/garantias.ts
@frontend/admin/src/hooks/use-garantias.ts
@frontend/admin/src/components/ui/table.tsx
@frontend/admin/src/components/ui/dialog.tsx
@frontend/admin/src/components/ui/badge.tsx

<interfaces>
<!-- Tipos criados no Plan 01 — executor deve ler os arquivos reais -->

De src/types/garantias.ts:
```typescript
export interface GarantiaUsina {
  id: string
  usina_id: string
  usina_nome: string
  data_inicio: string
  meses: number
  observacoes: string
  data_fim: string
  dias_restantes: number
  ativa: boolean
  criado_em: string
  atualizado_em: string
}

export interface GarantiaInput {
  data_inicio: string   // 'YYYY-MM-DD'
  meses: number         // min_value=1
  observacoes?: string
}

export interface PaginatedGarantias {
  count: number; next: string | null; previous: string | null
  results: GarantiaUsina[]
}
```

De src/hooks/use-garantias.ts:
```typescript
export function useGarantias(params): { data: PaginatedGarantias | null, loading, error, refetch }
```

API PUT /api/usinas/{id}/garantia/:
- Aceita: { data_inicio: 'YYYY-MM-DD', meses: number (>=1), observacoes?: string }
- Retorna: GarantiaUsina completa com data_fim e dias_restantes calculados
- Comportamento: cria se nao existe, substitui se ja existe (upsert via update_or_create)
- Endpoint: /api/usinas/{USINA_ID}/garantia/ (NAO /api/garantias/{id}/)

API GET /api/garantias/:
- Filtros: ?filtro=ativas, ?filtro=vencendo, ?filtro=vencidas
- Retorna PaginatedGarantias
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GarantiasTable e GarantiasPage com filtros e indicador vermelho (FE-09, FE-10)</name>
  <files>
    frontend/admin/src/components/garantias/GarantiasTable.tsx,
    frontend/admin/src/pages/GarantiasPage.tsx
  </files>
  <read_first>
    frontend/admin/src/types/garantias.ts,
    frontend/admin/src/hooks/use-garantias.ts,
    frontend/admin/src/components/ui/table.tsx,
    frontend/admin/src/components/ui/badge.tsx,
    frontend/admin/src/components/ui/select.tsx,
    frontend/admin/src/pages/GarantiasPage.tsx
  </read_first>
  <action>
1. Criar `frontend/admin/src/components/garantias/GarantiasTable.tsx`:
   - Props: `{ garantias: GarantiaUsina[], onEdit: (garantia: GarantiaUsina) => void }`
   - Usar Table, TableHeader, TableBody, TableRow, TableHead, TableCell do shadcn
   - Colunas: Usina (usina_nome), Data Inicio, Data Fim, Meses, Dias Restantes, Status, Acoes
   - **Indicador de vencimento proximo (FE-10):**
     - Calcular `isVencendoLogo = garantia.ativa && garantia.dias_restantes < 30`
     - Se isVencendoLogo: aplicar `className="bg-red-50"` na TableRow inteira E exibir Badge "Vencendo" com `className="bg-red-100 text-red-800"` na coluna Dias Restantes
     - Se ativa e nao vencendo: Badge "Ativa" com `className="bg-green-100 text-green-800"`
     - Se nao ativa: Badge "Vencida" com `className="bg-red-100 text-red-800"`
   - Coluna Dias Restantes: `{garantia.dias_restantes} dias` — com Badge de status ao lado
   - Coluna Data Inicio: `new Date(garantia.data_inicio + 'T00:00:00').toLocaleDateString('pt-BR')` — o T00:00:00 evita timezone shift
   - Coluna Data Fim: `new Date(garantia.data_fim + 'T00:00:00').toLocaleDateString('pt-BR')`
   - Coluna Acoes: botao "Editar" (variant="outline", size="sm") que chama `onEdit(garantia)`
   - Se array vazio: `<TableRow><TableCell colSpan={7} className="text-center text-muted-foreground">Nenhuma garantia encontrada</TableCell></TableRow>`

2. Substituir INTEIRAMENTE `frontend/admin/src/pages/GarantiasPage.tsx` (placeholder atual):
   - Estados locais: `filtro: string` (default ''), `page: number` (default 1), `editingGarantia: GarantiaUsina | null` (default null)
   - Chamar `useGarantias({ filtro: filtro || undefined, page })`
   - Filtro no topo:
     - Select de filtro: opcoes "Todas" (value ''), "Ativas" (value 'ativas'), "Vencendo em 30 dias" (value 'vencendo'), "Vencidas" (value 'vencidas')
     - Ao mudar filtro, resetar page para 1
   - Abaixo: `<GarantiasTable garantias={data?.results ?? []} onEdit={setEditingGarantia} />`
   - Paginacao: mesmo padrao do UsinasPage — PaginationPrevious/PaginationNext com disabled baseado em data.previous/data.next. Texto "Pagina {page} de {Math.ceil((data?.count ?? 0) / 20)}"
   - Loading: spinner centralizado
   - Error: mensagem de erro
   - Modal: `<GarantiaFormDialog garantia={editingGarantia} open={!!editingGarantia} onClose={() => setEditingGarantia(null)} onSuccess={() => { setEditingGarantia(null); refetch() }} />`
     (GarantiaFormDialog sera criado na Task 2)
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && grep -c "useGarantias" src/pages/GarantiasPage.tsx && grep -c "bg-red-50" src/components/garantias/GarantiasTable.tsx && grep -c "dias_restantes" src/components/garantias/GarantiasTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GarantiasPage.tsx importa e usa useGarantias com params de filtro
    - GarantiasPage.tsx tem Select de filtro com opcoes ativas/vencendo/vencidas
    - GarantiasPage.tsx tem paginacao
    - GarantiasTable.tsx renderiza 7 colunas
    - GarantiasTable.tsx aplica bg-red-50 na linha quando ativa E dias_restantes < 30 (FE-10)
    - GarantiasTable.tsx exibe Badge de status (Ativa/Vencendo/Vencida)
    - GarantiasTable.tsx formata datas com toLocaleDateString('pt-BR')
    - Estado vazio tratado
    - Estado loading e error tratados
  </acceptance_criteria>
  <done>Listagem de garantias com filtros, paginacao e indicador vermelho para vencimento proximo (< 30 dias).</done>
</task>

<task type="auto">
  <name>Task 2: GarantiaFormDialog com preview de data_fim em tempo real (FE-11)</name>
  <files>
    frontend/admin/src/components/garantias/GarantiaFormDialog.tsx
  </files>
  <read_first>
    frontend/admin/src/types/garantias.ts,
    frontend/admin/src/lib/api.ts,
    frontend/admin/src/components/ui/dialog.tsx,
    frontend/admin/src/components/ui/input.tsx,
    frontend/admin/src/components/ui/label.tsx
  </read_first>
  <action>
Criar `frontend/admin/src/components/garantias/GarantiaFormDialog.tsx` (per D-03):
   - Props: `{ garantia: GarantiaUsina | null, open: boolean, onClose: () => void, onSuccess: () => void }`
   - Usar Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter do shadcn
   - Estados locais: `dataInicio: string` (default ''), `meses: string` (default '12'), `observacoes: string` (default ''), `saving: boolean`, `error: string | null`
   - `useEffect` que popula os campos quando `garantia` muda:
     - Se garantia !== null: setar dataInicio = garantia.data_inicio, meses = String(garantia.meses), observacoes = garantia.observacoes
     - Se garantia === null: manter defaults

   - **Preview de data_fim em tempo real (D-03):**
     ```typescript
     const dataFimPreview = useMemo(() => {
       if (!dataInicio || !meses || parseInt(meses) < 1) return null
       const d = new Date(dataInicio + 'T00:00:00')  // T00:00:00 evita timezone shift
       d.setMonth(d.getMonth() + parseInt(meses))
       return d.toLocaleDateString('pt-BR')
     }, [dataInicio, meses])
     ```
     Exibir abaixo dos campos do form: `{dataFimPreview && <p className="text-sm text-muted-foreground">Data fim prevista: <span className="font-medium">{dataFimPreview}</span></p>}`

   - Validacao local antes de submit:
     - dataInicio nao pode ser vazio — error "Data de inicio e obrigatoria"
     - meses deve ser inteiro >= 1 — error "Duracao deve ser pelo menos 1 mes"

   - handleSubmit:
     - setSaving(true), setError(null)
     - Endpoint: `api.put(\`/api/usinas/${garantia!.usina_id}/garantia/\`, { data_inicio: dataInicio, meses: parseInt(meses), observacoes })`
     - IMPORTANTE: o endpoint e `/api/usinas/{USINA_ID}/garantia/` — NAO `/api/garantias/{id}/`
     - Em sucesso: `toast.success('Garantia salva com sucesso')` (importar toast de sonner)
     - Chamar onSuccess()
     - Em erro: `toast.error('Erro ao salvar garantia')`, setError('Erro ao salvar')
     - finally: setSaving(false)

   - Layout do Dialog:
     - DialogTitle: "Editar Garantia"
     - DialogDescription: `garantia?.usina_nome` — nome da usina
     - Campo "Data de Inicio": `<Input type="date" value={dataInicio} onChange={...} />`
       O input type="date" ja retorna formato YYYY-MM-DD que a API espera — NAO converter.
     - Campo "Duracao (meses)": `<Input type="number" min="1" step="1" value={meses} onChange={...} />`
     - Campo "Observacoes": `<Input value={observacoes} onChange={...} placeholder="Observacoes (opcional)" />`
       Pode ser `<textarea>` com className do Input se quiser multi-line — decisao do executor.
     - Preview de data_fim (abaixo dos campos, antes dos botoes)
     - Se error: `<p className="text-sm text-destructive">{error}</p>`
     - DialogFooter: botao "Cancelar" (variant="outline", onClick=onClose) e botao "Salvar" (disabled={saving})
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -20 && grep -c "api.put" src/components/garantias/GarantiaFormDialog.tsx && grep -c "dataFimPreview" src/components/garantias/GarantiaFormDialog.tsx && grep -c "T00:00:00" src/components/garantias/GarantiaFormDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GarantiaFormDialog.tsx usa api.put (NAO api.patch) para /api/usinas/{usina_id}/garantia/
    - GarantiaFormDialog.tsx tem campos: data_inicio (type="date"), meses (type="number" min="1"), observacoes
    - GarantiaFormDialog.tsx calcula dataFimPreview com useMemo usando new Date(dataInicio + 'T00:00:00')
    - GarantiaFormDialog.tsx exibe preview de data_fim abaixo dos campos
    - GarantiaFormDialog.tsx valida data_inicio nao vazia e meses >= 1
    - GarantiaFormDialog.tsx importa toast de sonner para feedback
    - GarantiaFormDialog.tsx chama onSuccess() apos PUT bem-sucedido
    - npm run build compila sem erros
  </acceptance_criteria>
  <done>Modal de garantia funcional com preview em tempo real, validacao e feedback toast. PUT para endpoint correto /api/usinas/{id}/garantia/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual das paginas de usinas e garantias</name>
  <files></files>
  <action>Verificacao humana das paginas implementadas nos Plans 02 e 03.</action>
  <what-built>Paginas completas de usinas (listagem, detalhe, edicao) e garantias (listagem, indicador vermelho, modal de edicao com preview de data_fim).</what-built>
  <how-to-verify>
    1. Acesse http://localhost:5173/garantias (com Vite dev server rodando via `cd frontend/admin && npm run dev`)
    2. Verifique que a tabela de garantias carrega com colunas: Usina, Data Inicio, Data Fim, Meses, Dias Restantes, Status, Acoes
    3. Use o filtro para selecionar "Vencendo em 30 dias" — apenas garantias com < 30 dias devem aparecer, com linha vermelha
    4. Clique "Editar" em uma garantia — o modal deve abrir com campos preenchidos
    5. Altere a data de inicio ou meses no modal — o preview de "Data fim prevista" deve atualizar em tempo real
    6. Salve a garantia — deve aparecer toast de sucesso e a tabela deve atualizar

    Tambem verificar a pagina de usinas:
    7. Acesse http://localhost:5173/usinas
    8. Verifique filtros por provedor e status de garantia
    9. Clique em uma usina — detalhe deve exibir cards e inversores
    10. Clique "Editar" na listagem — modal deve abrir para editar nome/capacidade
  </how-to-verify>
  <verify>Verificacao manual pelo usuario</verify>
  <done>Usuario confirmou que as paginas funcionam corretamente.</done>
  <resume-signal>Digite "approved" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> API | Campos data_inicio, meses e observacoes do formulario enviados via PUT |
| Date calculation | Preview de data_fim calculado no cliente — meramente informativo, API calcula server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T (Tampering) | GarantiaFormDialog.tsx | mitigate | Validacao local: data_inicio obrigatoria, meses >= 1. Backend valida via GarantiaUsinaEscritaSerializer (min_value=1, DateField). Input type="date" garante formato YYYY-MM-DD. |
| T-05-08 | R (Repudiation) | GarantiaFormDialog.tsx | accept | Nao ha log de auditoria de quem editou garantia — admin unico, risco aceitavel para v1. |
| T-05-09 | I (Information Disclosure) | GarantiasTable.tsx | mitigate | Erro generico em catch — nao exibir mensagem do servidor. Dados de garantia sao internos (admin-only). |
| T-05-10 | T (Tampering) | Date preview | accept | Preview usa calculo JS nativo. Discrepancia entre preview e calculo server-side e possivel (meses com >28 dias), mas e aceitavel — a API e fonte de verdade. |
</threat_model>

<verification>
1. `npm run build` compila sem erros
2. GarantiasPage exibe tabela filtrada com indicador vermelho para vencimento proximo
3. GarantiaFormDialog faz PUT para /api/usinas/{id}/garantia/ (NAO /api/garantias/)
4. Preview de data_fim atualiza em tempo real ao mudar data_inicio ou meses
5. Feedback via toast apos salvar
6. Listagem re-fetcha apos edicao
</verification>

<success_criteria>
- Tabela de garantias com filtro por status (ativas/vencendo/vencidas)
- Indicador visual vermelho (bg-red-50 na linha + Badge "Vencendo") para dias_restantes < 30
- Modal de garantia com campos data_inicio, meses, observacoes
- Preview de data_fim calculado em tempo real no modal
- PUT para /api/usinas/{usina_id}/garantia/ com feedback toast
- Listagem atualiza automaticamente apos salvar
- Build sem erros TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/05-usinas-garantias/05-03-SUMMARY.md`
</output>
