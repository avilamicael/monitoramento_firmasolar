---
phase: 05-usinas-garantias
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/admin/src/types/usinas.ts
  - frontend/admin/src/types/garantias.ts
  - frontend/admin/src/hooks/use-usinas.ts
  - frontend/admin/src/hooks/use-garantias.ts
  - frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx
  - frontend/admin/src/App.tsx
  - frontend/admin/src/components/ui/table.tsx
  - frontend/admin/src/components/ui/badge.tsx
  - frontend/admin/src/components/ui/dialog.tsx
  - frontend/admin/src/components/ui/select.tsx
  - frontend/admin/src/components/ui/pagination.tsx
  - frontend/admin/src/components/ui/sonner.tsx
autonomous: true
requirements: [FE-06, FE-07, FE-08, FE-09, FE-10, FE-11]

must_haves:
  truths:
    - "Componentes shadcn table, badge, dialog, select, pagination e sonner estao instalados e importaveis"
    - "Tipos TypeScript para UsinaResumo, UsinaDetalhe, GarantiaUsina existem e refletem os contratos da API"
    - "Hooks useUsinas, useUsina e useGarantias retornam data/loading/error/refetch"
    - "Rota /usinas/:id esta registrada no router e renderiza UsinaDetalhePage"
    - "StatusGarantiaBadge renderiza 3 variantes visuais distintas"
  artifacts:
    - path: "frontend/admin/src/types/usinas.ts"
      provides: "UsinaResumo, UsinaDetalhe, InversorResumo, SnapshotUsina, PaginatedUsinas"
      exports: ["UsinaResumo", "UsinaDetalhe", "InversorResumo", "SnapshotUsina", "PaginatedUsinas"]
    - path: "frontend/admin/src/types/garantias.ts"
      provides: "GarantiaUsina, GarantiaInput, PaginatedGarantias"
      exports: ["GarantiaUsina", "GarantiaInput", "PaginatedGarantias"]
    - path: "frontend/admin/src/hooks/use-usinas.ts"
      provides: "useUsinas(params), useUsina(id)"
      exports: ["useUsinas", "useUsina"]
    - path: "frontend/admin/src/hooks/use-garantias.ts"
      provides: "useGarantias(params)"
      exports: ["useGarantias"]
    - path: "frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx"
      provides: "Badge colorido para ativa/vencida/sem_garantia"
      exports: ["StatusGarantiaBadge"]
    - path: "frontend/admin/src/App.tsx"
      provides: "Rota /usinas/:id registrada"
      contains: "usinas/:id"
  key_links:
    - from: "frontend/admin/src/hooks/use-usinas.ts"
      to: "/api/usinas/"
      via: "api.get no useEffect"
      pattern: "api\\.get.*api/usinas"
    - from: "frontend/admin/src/hooks/use-garantias.ts"
      to: "/api/garantias/"
      via: "api.get no useEffect"
      pattern: "api\\.get.*api/garantias"
---

<objective>
Instalar componentes shadcn necessarios, criar tipos TypeScript, hooks de data fetching e componente de badge de status — toda a fundacao que os Plans 02 e 03 consomem.

Purpose: Plans 02 e 03 precisam de tipos, hooks e componentes base para implementar as paginas. Sem esta fundacao, cada plan teria que ler serializers e inventar tipos, desperdicando contexto.
Output: Tipos exportados, hooks funcionais, componente de badge, rota de detalhe registrada, shadcn components instalados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-usinas-garantias/05-CONTEXT.md
@.planning/phases/05-usinas-garantias/05-RESEARCH.md
@.planning/phases/04-frontend-foundation/04-01-SUMMARY.md

@frontend/admin/src/lib/api.ts
@frontend/admin/src/App.tsx
@frontend/admin/package.json

<interfaces>
<!-- Contratos da API backend — verificados nos serializers -->

GET /api/usinas/ retorna:
```typescript
interface UsinaResumo {
  id: string
  nome: string
  provedor: string
  capacidade_kwp: number
  ativo: boolean
  status_garantia: 'ativa' | 'vencida' | 'sem_garantia'
  criado_em: string
  atualizado_em: string
}

interface PaginatedUsinas {
  count: number
  next: string | null
  previous: string | null
  results: UsinaResumo[]
}
```

GET /api/usinas/{id}/ retorna:
```typescript
interface InversorResumo {
  id: string
  numero_serie: string
  modelo: string
  id_inversor_provedor: string
}

interface SnapshotUsina {
  id: string
  coletado_em: string
  data_medicao: string
  potencia_kw: number
  energia_hoje_kwh: number
  energia_mes_kwh: number
  energia_total_kwh: number
  status: string
  qtd_inversores: number
  qtd_inversores_online: number
  qtd_alertas: number
}

interface UsinaDetalhe {
  id: string
  nome: string
  provedor: string
  capacidade_kwp: number
  ativo: boolean
  fuso_horario: string
  endereco: string
  status_garantia: 'ativa' | 'vencida' | 'sem_garantia'
  ultimo_snapshot: SnapshotUsina | null
  inversores: InversorResumo[]
  criado_em: string
  atualizado_em: string
}
```

GET /api/garantias/ retorna:
```typescript
interface GarantiaUsina {
  id: string
  usina_id: string
  usina_nome: string
  data_inicio: string
  meses: number
  observacoes: string
  data_fim: string
  dias_restantes: number
  ativa: boolean
  criado_em: string
  atualizado_em: string
}
```

PUT /api/usinas/{id}/garantia/ aceita:
```typescript
interface GarantiaInput {
  data_inicio: string   // 'YYYY-MM-DD'
  meses: number         // min_value=1
  observacoes?: string
}
```

De frontend/admin/src/lib/api.ts:
```typescript
export default api  // instancia axios com interceptors JWT
```

De frontend/admin/src/App.tsx:
```typescript
// Router com ProtectedLayout e children: dashboard, usinas, garantias, alertas
// ROUTE_TITLES: Record<string, string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar componentes shadcn e criar tipos TypeScript</name>
  <files>
    frontend/admin/src/types/usinas.ts,
    frontend/admin/src/types/garantias.ts,
    frontend/admin/src/components/ui/table.tsx,
    frontend/admin/src/components/ui/badge.tsx,
    frontend/admin/src/components/ui/dialog.tsx,
    frontend/admin/src/components/ui/select.tsx,
    frontend/admin/src/components/ui/pagination.tsx,
    frontend/admin/src/components/ui/sonner.tsx
  </files>
  <read_first>
    frontend/admin/package.json,
    frontend/admin/components.json,
    backend_monitoramento/api/serializers/usinas.py,
    backend_monitoramento/api/serializers/garantias.py
  </read_first>
  <action>
1. Instalar componentes shadcn (um unico comando):
   ```bash
   cd frontend/admin && npx shadcn@latest add table badge dialog select pagination sonner
   ```
   ATENCAO: usar `sonner`, NAO `toast` — toast nao existe no estilo radix-nova e retorna 404.

2. Criar `frontend/admin/src/types/usinas.ts` com as interfaces que refletem EXATAMENTE os serializers do backend:
   - `StatusGarantia = 'ativa' | 'vencida' | 'sem_garantia'` (tipo literal)
   - `UsinaResumo` — campos: id (string), nome, provedor, capacidade_kwp (number), ativo (boolean), status_garantia (StatusGarantia), criado_em (string), atualizado_em (string)
   - `InversorResumo` — campos: id (string), numero_serie, modelo, id_inversor_provedor
   - `SnapshotUsina` — campos: id (string), coletado_em, data_medicao, potencia_kw (number), energia_hoje_kwh, energia_mes_kwh, energia_total_kwh, status, qtd_inversores (number), qtd_inversores_online, qtd_alertas
   - `UsinaDetalhe` — campos: tudo de UsinaResumo + fuso_horario, endereco, ultimo_snapshot (SnapshotUsina | null), inversores (InversorResumo[])
   - `PaginatedUsinas` — campos: count (number), next (string | null), previous (string | null), results (UsinaResumo[])
   - `UsinaPatch` — campos: nome? (string), capacidade_kwp? (number) — ambos opcionais

3. Criar `frontend/admin/src/types/garantias.ts`:
   - `GarantiaUsina` — campos: id (string), usina_id (string), usina_nome (string), data_inicio (string), meses (number), observacoes (string), data_fim (string), dias_restantes (number), ativa (boolean), criado_em (string), atualizado_em (string)
   - `GarantiaInput` — campos: data_inicio (string), meses (number), observacoes? (string)
   - `PaginatedGarantias` — campos: count (number), next (string | null), previous (string | null), results (GarantiaUsina[])

Todos os tipos exportados com `export interface` (ou `export type` para StatusGarantia).
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/admin/src/components/ui/table.tsx existe
    - frontend/admin/src/components/ui/badge.tsx existe
    - frontend/admin/src/components/ui/dialog.tsx existe
    - frontend/admin/src/components/ui/select.tsx existe
    - frontend/admin/src/components/ui/pagination.tsx existe
    - frontend/admin/src/components/ui/sonner.tsx existe
    - frontend/admin/src/types/usinas.ts exporta UsinaResumo, UsinaDetalhe, PaginatedUsinas, UsinaPatch, StatusGarantia
    - frontend/admin/src/types/garantias.ts exporta GarantiaUsina, GarantiaInput, PaginatedGarantias
    - npm run build compila sem erros TypeScript
  </acceptance_criteria>
  <done>Componentes shadcn instalados, tipos TypeScript exportados, build sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Criar hooks de data fetching e StatusGarantiaBadge</name>
  <files>
    frontend/admin/src/hooks/use-usinas.ts,
    frontend/admin/src/hooks/use-garantias.ts,
    frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx
  </files>
  <read_first>
    frontend/admin/src/lib/api.ts,
    frontend/admin/src/types/usinas.ts,
    frontend/admin/src/types/garantias.ts,
    frontend/admin/src/components/ui/badge.tsx
  </read_first>
  <action>
1. Criar `frontend/admin/src/hooks/use-usinas.ts` com DOIS hooks (per D-07):

   **useUsinas(params):**
   - Parametro `params` com tipo `{ provedor?: string; ativo?: boolean; status_garantia?: StatusGarantia; page?: number }`
   - Estados: `data: PaginatedUsinas | null`, `loading: boolean`, `error: string | null`
   - `fetch` como `useCallback` que faz `api.get('/api/usinas/', { params })` e seta os estados
   - Dependencia do useCallback: `JSON.stringify(params)` para evitar loop infinito com objetos literais recriados a cada render
   - `useEffect` que chama `fetch()` quando o callback muda
   - Retorna `{ data, loading, error, refetch: fetch }`

   **useUsina(id):**
   - Parametro `id: string`
   - Estados: `data: UsinaDetalhe | null`, `loading: boolean`, `error: string | null`
   - `fetch` como `useCallback` que faz `api.get(\`/api/usinas/${id}/\`)`
   - `useEffect` que chama `fetch()` quando `id` muda
   - Retorna `{ data, loading, error, refetch: fetch }`

2. Criar `frontend/admin/src/hooks/use-garantias.ts`:

   **useGarantias(params):**
   - Parametro `params` com tipo `{ filtro?: 'ativas' | 'vencendo' | 'vencidas'; page?: number }`
   - Mesma estrutura do useUsinas: data (PaginatedGarantias | null), loading, error, refetch
   - Endpoint: `api.get('/api/garantias/', { params })`
   - Dependencia do useCallback: `JSON.stringify(params)`

3. Criar `frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx` (per D-04):

   - Props: `{ status: StatusGarantia }`
   - Constante CONFIG com mapeamento (NAO inline):
     - `ativa`: label "Ativa", className "bg-green-100 text-green-800 hover:bg-green-100"
     - `vencida`: label "Vencida", className "bg-red-100 text-red-800 hover:bg-red-100"
     - `sem_garantia`: label "Sem Garantia", className "bg-gray-100 text-gray-600 hover:bg-gray-100"
   - Renderiza `<Badge className={config.className}>{config.label}</Badge>` usando o Badge do shadcn
   - Exportar como named export: `export function StatusGarantiaBadge`

Todos os hooks importam `api` de `@/lib/api` e tipos de `@/types/usinas` ou `@/types/garantias`.
Nenhum `console.log` nos hooks. Catch generico: `setError('Erro ao carregar ...')` — nao expor detalhes do erro.
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/admin/src/hooks/use-usinas.ts exporta useUsinas e useUsina
    - frontend/admin/src/hooks/use-garantias.ts exporta useGarantias
    - frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx exporta StatusGarantiaBadge
    - useUsinas retorna { data, loading, error, refetch }
    - useUsina retorna { data, loading, error, refetch }
    - useGarantias retorna { data, loading, error, refetch }
    - StatusGarantiaBadge aceita prop status com tipo StatusGarantia
    - grep "bg-green-100" StatusGarantiaBadge.tsx retorna match (badge ativa)
    - grep "bg-red-100" StatusGarantiaBadge.tsx retorna match (badge vencida)
    - grep "bg-gray-100" StatusGarantiaBadge.tsx retorna match (badge sem_garantia)
    - npm run build compila sem erros
  </acceptance_criteria>
  <done>3 hooks de data fetching e componente StatusGarantiaBadge prontos para consumo pelos Plans 02 e 03.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar rota /usinas/:id e Toaster no App.tsx</name>
  <files>
    frontend/admin/src/App.tsx,
    frontend/admin/src/pages/UsinaDetalhePage.tsx
  </files>
  <read_first>
    frontend/admin/src/App.tsx
  </read_first>
  <action>
1. Criar `frontend/admin/src/pages/UsinaDetalhePage.tsx` como placeholder temporario (sera substituido pelo Plan 02):
   ```tsx
   export function UsinaDetalhePage() {
     return (
       <div className="p-6">
         <h1 className="text-2xl font-bold">Detalhe da Usina</h1>
         <p className="text-muted-foreground">Carregando...</p>
       </div>
     )
   }
   ```
   O placeholder e necessario para que a rota compile. Plan 02 substituira o conteudo completo.

2. Editar `frontend/admin/src/App.tsx`:
   - Adicionar import: `import { UsinaDetalhePage } from '@/pages/UsinaDetalhePage'`
   - Adicionar import: `import { Toaster } from '@/components/ui/sonner'`
   - Dentro dos `children` do ProtectedLayout, ANTES da rota `{ path: 'usinas', ... }`, adicionar:
     `{ path: 'usinas/:id', element: <UsinaDetalhePage /> }`
     IMPORTANTE: a rota `usinas/:id` deve vir ANTES de `usinas` no array children para evitar conflito de matching.
   - Adicionar `<Toaster />` no componente App(), ao lado do RouterProvider:
     ```tsx
     export default function App() {
       return (
         <>
           <RouterProvider router={router} />
           <Toaster />
         </>
       )
     }
     ```
     Isto habilita toast.success() e toast.error() via sonner em qualquer componente (necessario para feedback de mutacoes nos Plans 02 e 03).

NAO alterar ROUTE_TITLES — o breadcrumb do detalhe sera tratado na propria pagina de detalhe (Plan 02).
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -20 && grep -n "usinas/:id" src/App.tsx && grep -n "Toaster" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/admin/src/pages/UsinaDetalhePage.tsx existe e exporta UsinaDetalhePage
    - App.tsx contem rota "usinas/:id" com element UsinaDetalhePage
    - App.tsx importa e renderiza Toaster de @/components/ui/sonner
    - npm run build compila sem erros
  </acceptance_criteria>
  <done>Rota /usinas/:id registrada no router, Toaster habilitado para feedback de mutacoes, build sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Todas as chamadas HTTP passam pelo interceptor axios que injeta JWT |
| API response -> UI | Dados da API renderizados nos componentes — potencial XSS se nao tratado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information Disclosure) | hooks/use-usinas.ts | mitigate | Catch generico: setError('Erro ao carregar usinas') — nao expor mensagem do backend ao usuario |
| T-05-02 | T (Tampering) | types/usinas.ts | accept | Tipos TypeScript sao compile-time only; validacao real e server-side nos serializers DRF |
| T-05-03 | S (Spoofing) | App.tsx router | accept | ProtectedLayout ja verifica isAuthenticated — rota /usinas/:id herda esta protecao |
</threat_model>

<verification>
1. `npm run build` compila sem erros TypeScript
2. Todos os 6 componentes shadcn existem em `src/components/ui/`
3. Tipos em `src/types/` refletem os contratos dos serializers do backend
4. Hooks retornam a interface correta (data, loading, error, refetch)
5. Rota `/usinas/:id` existe no router
6. `Toaster` renderizado no App
</verification>

<success_criteria>
- Build sem erros TypeScript
- 6 componentes shadcn instalados (table, badge, dialog, select, pagination, sonner)
- 5 tipos exportados em usinas.ts, 3 em garantias.ts
- 3 hooks funcionais (useUsinas, useUsina, useGarantias)
- StatusGarantiaBadge com 3 variantes visuais
- Rota /usinas/:id registrada e compilando
- Toaster global habilitado
</success_criteria>

<output>
After completion, create `.planning/phases/05-usinas-garantias/05-01-SUMMARY.md`
</output>
