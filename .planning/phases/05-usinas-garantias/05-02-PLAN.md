---
phase: 05-usinas-garantias
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/admin/src/pages/UsinasPage.tsx
  - frontend/admin/src/pages/UsinaDetalhePage.tsx
  - frontend/admin/src/components/usinas/UsinasTable.tsx
  - frontend/admin/src/components/usinas/UsinaEditDialog.tsx
autonomous: true
requirements: [FE-06, FE-07, FE-08]

must_haves:
  truths:
    - "Administrador ve tabela paginada de usinas com badge de status de garantia"
    - "Administrador filtra usinas por provedor e status de garantia"
    - "Administrador clica em usina e ve detalhe com inversores e ultimo snapshot"
    - "Administrador edita nome e capacidade via modal e a listagem atualiza sem reload"
  artifacts:
    - path: "frontend/admin/src/pages/UsinasPage.tsx"
      provides: "Pagina de listagem de usinas com filtros e paginacao"
      min_lines: 30
    - path: "frontend/admin/src/pages/UsinaDetalhePage.tsx"
      provides: "Pagina de detalhe com card principal e tabela de inversores"
      min_lines: 50
    - path: "frontend/admin/src/components/usinas/UsinasTable.tsx"
      provides: "Tabela com colunas, badge de status, link para detalhe"
      min_lines: 40
    - path: "frontend/admin/src/components/usinas/UsinaEditDialog.tsx"
      provides: "Modal Dialog com form para PATCH nome e capacidade"
      min_lines: 40
  key_links:
    - from: "frontend/admin/src/pages/UsinasPage.tsx"
      to: "useUsinas hook"
      via: "import useUsinas from @/hooks/use-usinas"
      pattern: "useUsinas"
    - from: "frontend/admin/src/pages/UsinaDetalhePage.tsx"
      to: "useUsina hook"
      via: "import useUsina + useParams"
      pattern: "useUsina.*useParams"
    - from: "frontend/admin/src/components/usinas/UsinaEditDialog.tsx"
      to: "/api/usinas/{id}/"
      via: "api.patch"
      pattern: "api\\.patch.*usinas"
---

<objective>
Implementar as paginas de listagem e detalhe de usinas com filtros, paginacao, badge de garantia e modal de edicao.

Purpose: FE-06 (listagem com filtros e badge), FE-07 (detalhe com inversores e snapshot) e FE-08 (edicao nome/capacidade) sao o core de gestao de usinas.
Output: UsinasPage funcional com tabela filtrada, UsinaDetalhePage com card e inversores, UsinaEditDialog com PATCH e re-fetch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-usinas-garantias/05-CONTEXT.md
@.planning/phases/05-usinas-garantias/05-RESEARCH.md
@.planning/phases/05-usinas-garantias/05-01-SUMMARY.md

@frontend/admin/src/lib/api.ts
@frontend/admin/src/App.tsx
@frontend/admin/src/types/usinas.ts
@frontend/admin/src/hooks/use-usinas.ts
@frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx
@frontend/admin/src/components/ui/card.tsx

<interfaces>
<!-- Tipos criados no Plan 01 — executor deve ler os arquivos reais -->

De src/types/usinas.ts:
```typescript
export type StatusGarantia = 'ativa' | 'vencida' | 'sem_garantia'

export interface UsinaResumo {
  id: string; nome: string; provedor: string; capacidade_kwp: number
  ativo: boolean; status_garantia: StatusGarantia
  criado_em: string; atualizado_em: string
}

export interface UsinaDetalhe extends ... {
  fuso_horario: string; endereco: string
  ultimo_snapshot: SnapshotUsina | null
  inversores: InversorResumo[]
}

export interface UsinaPatch {
  nome?: string; capacidade_kwp?: number
}

export interface PaginatedUsinas {
  count: number; next: string | null; previous: string | null
  results: UsinaResumo[]
}
```

De src/hooks/use-usinas.ts:
```typescript
export function useUsinas(params): { data: PaginatedUsinas | null, loading, error, refetch }
export function useUsina(id: string): { data: UsinaDetalhe | null, loading, error, refetch }
```

De src/components/usinas/StatusGarantiaBadge.tsx:
```typescript
export function StatusGarantiaBadge({ status }: { status: StatusGarantia }): JSX.Element
```

API PATCH /api/usinas/{id}/:
- Aceita APENAS: { nome?: string, capacidade_kwp?: number }
- PUT retorna 405 — usar SOMENTE PATCH
- Resposta: objeto UsinaDetalhe completo (pode ser usado para atualizar estado local)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UsinasTable e UsinasPage com filtros, paginacao e badge (FE-06)</name>
  <files>
    frontend/admin/src/components/usinas/UsinasTable.tsx,
    frontend/admin/src/pages/UsinasPage.tsx
  </files>
  <read_first>
    frontend/admin/src/types/usinas.ts,
    frontend/admin/src/hooks/use-usinas.ts,
    frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx,
    frontend/admin/src/components/ui/table.tsx,
    frontend/admin/src/components/ui/select.tsx,
    frontend/admin/src/components/ui/pagination.tsx,
    frontend/admin/src/pages/UsinasPage.tsx
  </read_first>
  <action>
1. Criar `frontend/admin/src/components/usinas/UsinasTable.tsx`:
   - Props: `{ usinas: UsinaResumo[], onEdit: (usina: UsinaResumo) => void }`
   - Usar componentes Table, TableHeader, TableBody, TableRow, TableHead, TableCell do shadcn
   - Colunas: Nome (link para `/usinas/${usina.id}` via react-router Link), Provedor, Capacidade (kWp), Status Garantia (StatusGarantiaBadge), Acoes
   - Coluna Nome: `<Link to={\`/usinas/${usina.id}\`} className="font-medium text-primary hover:underline">{usina.nome}</Link>`
   - Coluna Capacidade: formatar como `{usina.capacidade_kwp} kWp`
   - Coluna Status Garantia: `<StatusGarantiaBadge status={usina.status_garantia} />`
   - Coluna Acoes: botao "Editar" (variant="outline", size="sm") que chama `onEdit(usina)`
   - Se array vazio: exibir `<TableRow><TableCell colSpan={5} className="text-center text-muted-foreground">Nenhuma usina encontrada</TableCell></TableRow>`

2. Substituir INTEIRAMENTE `frontend/admin/src/pages/UsinasPage.tsx` (o conteudo atual e placeholder):
   - Estados locais: `provedor: string` (default ''), `statusGarantia: string` (default ''), `page: number` (default 1), `editingUsina: UsinaResumo | null` (default null)
   - Chamar `useUsinas({ provedor: provedor || undefined, status_garantia: statusGarantia || undefined, page })`
   - Filtros no topo da pagina:
     - Select de provedor: opcoes "Todos" (value ''), "Solis" (value 'solis'), "Growatt" (value 'growatt'). Usar shadcn Select com SelectTrigger, SelectContent, SelectItem.
     - Select de status garantia: opcoes "Todos" (value ''), "Ativa" (value 'ativa'), "Vencida" (value 'vencida'), "Sem Garantia" (value 'sem_garantia'). Mesmo padrao.
     - Ao mudar qualquer filtro, resetar `page` para 1.
   - Abaixo dos filtros: `<UsinasTable usinas={data?.results ?? []} onEdit={setEditingUsina} />`
   - Paginacao abaixo da tabela: usar shadcn Pagination (PaginationContent, PaginationItem, PaginationPrevious, PaginationNext).
     - PaginationPrevious: disabled se `!data?.previous`, onClick seta `page - 1`
     - PaginationNext: disabled se `!data?.next`, onClick seta `page + 1`
     - Exibir texto "Pagina {page} de {Math.ceil((data?.count ?? 0) / 20)}" entre os botoes
   - Estados de loading e error:
     - Se loading: exibir `<div className="flex justify-center py-8"><Loader2Icon className="size-6 animate-spin text-muted-foreground" /></div>`
     - Se error: exibir `<div className="py-8 text-center text-destructive">{error}</div>`
   - Modal de edicao: `<UsinaEditDialog usina={editingUsina} open={!!editingUsina} onClose={() => setEditingUsina(null)} onSuccess={() => { setEditingUsina(null); refetch() }} />`
     (UsinaEditDialog sera criado na Task 2 — importar mesmo que ainda nao exista; o TypeScript so vai verificar no build final)
   - Import Link de react-router para usar no UsinasTable.

IMPORTANTE: A API retorna page_size=20 por padrao. Calcular total de paginas como `Math.ceil(count / 20)`.
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && grep -c "useUsinas" src/pages/UsinasPage.tsx && grep -c "StatusGarantiaBadge" src/components/usinas/UsinasTable.tsx && grep -c "TableRow" src/components/usinas/UsinasTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UsinasPage.tsx importa e usa useUsinas com params de filtro
    - UsinasPage.tsx tem Select de provedor com opcoes solis/growatt
    - UsinasPage.tsx tem Select de status_garantia com opcoes ativa/vencida/sem_garantia
    - UsinasPage.tsx tem paginacao com controle de page state
    - UsinasTable.tsx renderiza Table com 5 colunas (Nome, Provedor, Capacidade, Status Garantia, Acoes)
    - UsinasTable.tsx usa StatusGarantiaBadge para coluna de status
    - UsinasTable.tsx usa Link do react-router na coluna Nome apontando para /usinas/{id}
    - Estado vazio tratado com mensagem "Nenhuma usina encontrada"
    - Estado loading tratado com spinner
    - Estado error tratado com mensagem
  </acceptance_criteria>
  <done>UsinasPage exibe tabela paginada com filtros por provedor e status de garantia, badge colorido e link para detalhe.</done>
</task>

<task type="auto">
  <name>Task 2: UsinaEditDialog e UsinaDetalhePage (FE-07, FE-08)</name>
  <files>
    frontend/admin/src/components/usinas/UsinaEditDialog.tsx,
    frontend/admin/src/pages/UsinaDetalhePage.tsx
  </files>
  <read_first>
    frontend/admin/src/types/usinas.ts,
    frontend/admin/src/hooks/use-usinas.ts,
    frontend/admin/src/lib/api.ts,
    frontend/admin/src/components/ui/dialog.tsx,
    frontend/admin/src/components/ui/card.tsx,
    frontend/admin/src/components/ui/table.tsx,
    frontend/admin/src/components/usinas/StatusGarantiaBadge.tsx
  </read_first>
  <action>
1. Criar `frontend/admin/src/components/usinas/UsinaEditDialog.tsx` (per D-02):
   - Props: `{ usina: UsinaResumo | null, open: boolean, onClose: () => void, onSuccess: () => void }`
   - Usar Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter do shadcn
   - Estados locais: `nome: string`, `capacidadeKwp: string` (string para o input), `saving: boolean`, `error: string | null`
   - `useEffect` que popula nome e capacidadeKwp quando `usina` muda (se usina !== null)
   - Validacao local antes de submit:
     - `nome` nao pode ser vazio — se vazio, setar error "Nome e obrigatorio"
     - `capacidadeKwp` deve ser numero positivo — se nao, setar error "Capacidade deve ser um numero positivo"
   - handleSubmit:
     - setSaving(true), setError(null)
     - `await api.patch(\`/api/usinas/${usina!.id}/\`, { nome, capacidade_kwp: parseFloat(capacidadeKwp) })`
     - Em sucesso: `toast.success('Usina atualizada com sucesso')` (importar toast de sonner)
     - Chamar `onSuccess()`
     - Em erro: `toast.error('Erro ao atualizar usina')`, setError('Erro ao salvar')
     - finally: setSaving(false)
   - Layout do Dialog:
     - DialogTitle: "Editar Usina"
     - DialogDescription: nome da usina sendo editada
     - Dois campos: Label "Nome" + Input value={nome}, Label "Capacidade (kWp)" + Input type="number" step="0.01" min="0" value={capacidadeKwp}
     - Se error: `<p className="text-sm text-destructive">{error}</p>`
     - DialogFooter: botao "Cancelar" (variant="outline", onClick=onClose) e botao "Salvar" (disabled={saving}, texto "Salvando..." quando saving)
   - NAO usar PUT — a API retorna 405 para PUT em /api/usinas/{id}/. Usar SOMENTE api.patch.

2. Substituir INTEIRAMENTE `frontend/admin/src/pages/UsinaDetalhePage.tsx` (per D-05):
   - Usar `useParams<{ id: string }>()` de react-router para obter o id
   - Chamar `useUsina(id!)` para buscar dados do detalhe
   - Se loading: spinner centralizado
   - Se error: mensagem de erro
   - Se data === null apos loading: "Usina nao encontrada"
   - Layout (per D-05):
     - Botao "Voltar" no topo: `<Link to="/usinas" className="..."><ArrowLeftIcon className="size-4" /> Voltar para Usinas</Link>` (usar ArrowLeft do lucide-react)
     - **Card principal** (usando Card, CardHeader, CardTitle, CardContent do shadcn):
       - CardTitle: nome da usina
       - CardContent em grid (2 colunas em lg, 1 em mobile):
         - Provedor: `data.provedor`
         - Capacidade: `{data.capacidade_kwp} kWp`
         - Status Garantia: `<StatusGarantiaBadge status={data.status_garantia} />`
         - Endereco: `data.endereco || '—'`
         - Fuso Horario: `data.fuso_horario`
     - **Card ultimo snapshot** (se `data.ultimo_snapshot !== null`):
       - CardTitle: "Ultimo Snapshot"
       - CardContent em grid:
         - Potencia: `{data.ultimo_snapshot.potencia_kw} kW`
         - Energia Hoje: `{data.ultimo_snapshot.energia_hoje_kwh} kWh`
         - Energia Mes: `{data.ultimo_snapshot.energia_mes_kwh} kWh`
         - Energia Total: `{data.ultimo_snapshot.energia_total_kwh} kWh`
         - Inversores Online: `{data.ultimo_snapshot.qtd_inversores_online}/{data.ultimo_snapshot.qtd_inversores}`
         - Alertas: `{data.ultimo_snapshot.qtd_alertas}`
         - Coletado em: `new Date(data.ultimo_snapshot.coletado_em).toLocaleString('pt-BR')`
       - Se `data.ultimo_snapshot === null`: exibir card com texto "Nenhum snapshot coletado ainda" em text-muted-foreground
       - USAR OPTIONAL CHAINING: `data.ultimo_snapshot?.potencia_kw` para prevenir TypeError
     - **Tabela de inversores** abaixo dos cards:
       - Titulo "Inversores ({data.inversores.length})"
       - Table com colunas: Numero de Serie, Modelo, ID Provedor
       - Se nao ha inversores: "Nenhum inversor associado"
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -20 && grep -c "api.patch" src/components/usinas/UsinaEditDialog.tsx && grep -c "useParams" src/pages/UsinaDetalhePage.tsx && grep -c "ultimo_snapshot" src/pages/UsinaDetalhePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UsinaEditDialog.tsx usa api.patch (NAO api.put) para /api/usinas/{id}/
    - UsinaEditDialog.tsx importa toast de sonner para feedback
    - UsinaEditDialog.tsx valida nome nao vazio e capacidade positiva antes de submeter
    - UsinaEditDialog.tsx chama onSuccess() apos PATCH bem-sucedido (permite re-fetch na pagina pai)
    - UsinaDetalhePage.tsx usa useParams para obter id da URL
    - UsinaDetalhePage.tsx usa useUsina(id) para buscar dados
    - UsinaDetalhePage.tsx tem Card principal com nome, provedor, capacidade, status garantia
    - UsinaDetalhePage.tsx tem Card de ultimo snapshot com optional chaining (ultimo_snapshot?)
    - UsinaDetalhePage.tsx trata ultimo_snapshot === null com mensagem alternativa
    - UsinaDetalhePage.tsx tem tabela de inversores com numero_serie, modelo, id_inversor_provedor
    - UsinaDetalhePage.tsx tem link de volta para /usinas
    - npm run build compila sem erros
  </acceptance_criteria>
  <done>Modal de edicao salva via PATCH com feedback toast, pagina de detalhe exibe cards + inversores + snapshot com tratamento de null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> API | Campos nome e capacidade_kwp do formulario de edicao enviados via PATCH |
| API response -> DOM | Dados renderizados em tabela e cards — React escapa HTML por padrao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T (Tampering) | UsinaEditDialog.tsx | mitigate | Validacao local: nome nao vazio, capacidade positiva. Backend valida server-side via UsinaPatchSerializer. Usar PATCH (nao PUT) — API bloqueia PUT com 405. |
| T-05-05 | I (Information Disclosure) | UsinaDetalhePage.tsx | mitigate | Erro generico no catch do hook — nao exibir stack trace ou mensagem do servidor |
| T-05-06 | D (Denial of Service) | UsinasPage filtros | accept | Re-fetch a cada mudanca de filtro e baixo risco — admin unico, volume pequeno de usinas |
</threat_model>

<verification>
1. `npm run build` compila sem erros
2. UsinasPage exibe tabela com filtros e paginacao
3. UsinasTable renderiza badge de status via StatusGarantiaBadge
4. UsinaEditDialog faz PATCH (nao PUT) com feedback via toast
5. UsinaDetalhePage exibe card principal + card snapshot (com tratamento null) + tabela inversores
6. Navegacao ida e volta: /usinas -> /usinas/:id -> /usinas funciona
</verification>

<success_criteria>
- Tabela de usinas paginada com filtro por provedor e status de garantia
- Badge colorido de status de garantia em cada linha
- Link de cada usina leva a /usinas/:id com detalhe completo
- Detalhe exibe inversores e ultimo snapshot (com tratamento de null)
- Modal de edicao salva nome e capacidade via PATCH com feedback toast
- Listagem atualiza automaticamente apos edicao (re-fetch)
- Build sem erros TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/05-usinas-garantias/05-02-SUMMARY.md`
</output>
