---
phase: 06-dashboard-anal-tico-alertas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/admin/package.json
  - frontend/admin/src/types/analytics.ts
  - frontend/admin/src/types/alertas.ts
  - frontend/admin/src/hooks/use-analytics.ts
  - frontend/admin/src/hooks/use-alertas.ts
  - frontend/admin/src/components/dashboard/PotenciaPieChart.tsx
  - frontend/admin/src/components/dashboard/RankingTable.tsx
  - frontend/admin/src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - FE-12
  - FE-13
  - FE-16
must_haves:
  truths:
    - "DashboardPage exibe grafico de pizza com potencia media por fabricante usando dados reais da API"
    - "DashboardPage exibe tabela de ranking top 5 fabricantes por inversores ativos"
    - "Dados do dashboard sao atualizados automaticamente a cada 10 minutos via polling"
    - "Clicar em um fabricante no ranking armazena selectedProvedor no state do DashboardPage"
  artifacts:
    - path: "frontend/admin/src/types/analytics.ts"
      provides: "Tipos PotenciaResponse, RankingResponse, MapaUsina"
      exports: ["ProvedorPotencia", "PotenciaResponse", "ProvedorRanking", "RankingResponse", "MapaUsina"]
    - path: "frontend/admin/src/types/alertas.ts"
      provides: "Tipos AlertaResumo, AlertaDetalhe, AlertaPatch, PaginatedAlertas"
      exports: ["EstadoAlerta", "NivelAlerta", "AlertaResumo", "AlertaDetalhe", "AlertaPatch", "PaginatedAlertas"]
    - path: "frontend/admin/src/hooks/use-analytics.ts"
      provides: "Hooks useAnalyticsPotencia, useAnalyticsRanking, useAnalyticsMapa com polling"
      exports: ["useAnalyticsPotencia", "useAnalyticsRanking", "useAnalyticsMapa"]
    - path: "frontend/admin/src/hooks/use-alertas.ts"
      provides: "Hooks useAlertas com filtros e useAlerta por id"
      exports: ["useAlertas", "useAlerta"]
    - path: "frontend/admin/src/components/dashboard/PotenciaPieChart.tsx"
      provides: "Grafico de pizza Recharts com potencia por provedor"
      exports: ["PotenciaPieChart"]
    - path: "frontend/admin/src/components/dashboard/RankingTable.tsx"
      provides: "Tabela ranking top 5 fabricantes, clicavel para filtro"
      exports: ["RankingTable"]
    - path: "frontend/admin/src/pages/DashboardPage.tsx"
      provides: "Pagina principal com grid: pizza + ranking + slot para mapa"
  key_links:
    - from: "frontend/admin/src/hooks/use-analytics.ts"
      to: "/api/analytics/potencia/"
      via: "api.get com setInterval polling 600000ms"
      pattern: "setInterval.*600.?000"
    - from: "frontend/admin/src/components/dashboard/PotenciaPieChart.tsx"
      to: "recharts"
      via: "import PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer"
      pattern: "from 'recharts'"
    - from: "frontend/admin/src/pages/DashboardPage.tsx"
      to: "frontend/admin/src/hooks/use-analytics.ts"
      via: "useAnalyticsPotencia() e useAnalyticsRanking()"
      pattern: "useAnalyticsPotencia|useAnalyticsRanking"
---

<objective>
Instalar dependencias (recharts, react-leaflet, leaflet), criar tipos TypeScript, hooks de data fetching com polling de 10min, e implementar o DashboardPage com grafico de pizza e tabela de ranking.

Purpose: Entregar a base de dados e a metade superior do dashboard (pizza + ranking) com polling funcional — o mapa sera adicionado no Plan 02.
Output: Tipos, hooks, PotenciaPieChart, RankingTable, DashboardPage funcional com dados reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-anal-tico-alertas/06-CONTEXT.md
@.planning/phases/06-dashboard-anal-tico-alertas/06-RESEARCH.md

@frontend/admin/src/hooks/use-usinas.ts
@frontend/admin/src/hooks/use-garantias.ts
@frontend/admin/src/types/usinas.ts
@frontend/admin/src/types/garantias.ts
@frontend/admin/src/lib/api.ts
@frontend/admin/src/pages/DashboardPage.tsx
@frontend/admin/src/components/usinas/UsinasTable.tsx
@frontend/admin/package.json
@backend_monitoramento/api/views/analytics.py
@backend_monitoramento/api/serializers/analytics.py
@backend_monitoramento/api/serializers/alertas.py
@backend_monitoramento/api/views/alertas.py

<interfaces>
<!-- Formatos de resposta da API que os hooks consomem -->

GET /api/analytics/potencia/ retorna:
{
  "media_geral_kw": number | null,
  "por_provedor": [{ "provedor": string, "media_kw": number | null, "usinas_ativas": number }]
}

GET /api/analytics/ranking-fabricantes/ retorna:
{
  "ranking": [{ "provedor": string, "inversores_ativos": number }]
}

GET /api/analytics/mapa/ retorna array plano:
[{ "id": string, "nome": string, "provedor": string, "latitude": number|null, "longitude": number|null, "ativo": boolean, "status": string }]

GET /api/alertas/ retorna paginado:
{
  "count": number, "next": string|null, "previous": string|null,
  "results": [{ "id": string, "usina": string, "usina_nome": string, "mensagem": string, "nivel": string, "estado": string, "inicio": string, "fim": string|null, "com_garantia": boolean, "criado_em": string, "atualizado_em": string }]
}

GET /api/alertas/{id}/ retorna:
{ ...campos acima + "catalogo_alarme": number|null, "id_alerta_provedor": string, "equipamento_sn": string, "sugestao": string, "anotacoes": string }

PATCH /api/alertas/{id}/ aceita: { "estado"?: string, "anotacoes"?: string }

SnapshotUsina.STATUS_CHOICES: 'normal' | 'aviso' | 'offline' | 'construcao'
MapaUsina.status: 'sem_dados' | 'normal' | 'aviso' | 'offline' | 'construcao'
Alerta.ESTADO_CHOICES: 'ativo' | 'em_atendimento' | 'resolvido'
Alerta.NIVEL_CHOICES: 'info' | 'aviso' | 'importante' | 'critico'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar pacotes, criar tipos TypeScript e hooks com polling</name>
  <files>
    frontend/admin/package.json,
    frontend/admin/src/types/analytics.ts,
    frontend/admin/src/types/alertas.ts,
    frontend/admin/src/hooks/use-analytics.ts,
    frontend/admin/src/hooks/use-alertas.ts
  </files>
  <read_first>
    frontend/admin/package.json,
    frontend/admin/src/hooks/use-usinas.ts,
    frontend/admin/src/hooks/use-garantias.ts,
    frontend/admin/src/types/usinas.ts,
    frontend/admin/src/lib/api.ts,
    backend_monitoramento/api/views/analytics.py,
    backend_monitoramento/api/serializers/alertas.py,
    backend_monitoramento/api/views/alertas.py,
    backend_monitoramento/api/filters/alertas.py
  </read_first>
  <action>
1. Instalar pacotes (per D-01, D-02):
   ```bash
   cd frontend/admin
   npm install recharts react-leaflet leaflet
   npm install --save-dev @types/leaflet
   ```

2. Criar `frontend/admin/src/types/analytics.ts` com tipos exatos (per D-01, D-07):
   ```typescript
   export interface ProvedorPotencia {
     provedor: string
     media_kw: number | null
     usinas_ativas: number
   }
   export interface PotenciaResponse {
     media_geral_kw: number | null
     por_provedor: ProvedorPotencia[]
   }
   export interface ProvedorRanking {
     provedor: string
     inversores_ativos: number
   }
   export interface RankingResponse {
     ranking: ProvedorRanking[]
   }
   export interface MapaUsina {
     id: string
     nome: string
     provedor: string
     latitude: number | null
     longitude: number | null
     ativo: boolean
     status: 'sem_dados' | 'normal' | 'aviso' | 'offline' | 'construcao'
   }
   ```

3. Criar `frontend/admin/src/types/alertas.ts` com tipos exatos (per D-04):
   ```typescript
   export type EstadoAlerta = 'ativo' | 'em_atendimento' | 'resolvido'
   export type NivelAlerta = 'info' | 'aviso' | 'importante' | 'critico'
   export interface AlertaResumo {
     id: string
     usina: string
     usina_nome: string
     mensagem: string
     nivel: NivelAlerta
     estado: EstadoAlerta
     inicio: string
     fim: string | null
     com_garantia: boolean
     criado_em: string
     atualizado_em: string
   }
   export interface AlertaDetalhe extends AlertaResumo {
     catalogo_alarme: number | null
     id_alerta_provedor: string
     equipamento_sn: string
     sugestao: string
     anotacoes: string
   }
   export interface AlertaPatch {
     estado?: EstadoAlerta
     anotacoes?: string
   }
   export interface PaginatedAlertas {
     count: number
     next: string | null
     previous: string | null
     results: AlertaResumo[]
   }
   ```

4. Criar `frontend/admin/src/hooks/use-analytics.ts` (per D-03, D-07):
   - Seguir EXATAMENTE o pattern de `use-usinas.ts`: useState + useCallback + useEffect
   - Adicionar polling: `const POLL_INTERVAL = 10 * 60 * 1000` (600_000ms)
   - No useEffect, chamar `void fetch()` imediatamente, depois `const timer = setInterval(() => void fetch(), POLL_INTERVAL)`, return `() => clearInterval(timer)`
   - `useAnalyticsPotencia()`: GET /api/analytics/potencia/, retorna `{ data: PotenciaResponse | null, loading, error, refetch }`
   - `useAnalyticsRanking()`: GET /api/analytics/ranking-fabricantes/, retorna `{ data: RankingResponse | null, loading, error, refetch }`
   - `useAnalyticsMapa()`: GET /api/analytics/mapa/, retorna `{ data: MapaUsina[] | null, loading, error, refetch }` (API retorna array plano, sem paginacao)
   - Nenhum parametro dinamico nos 3 hooks — useCallback sem deps (array vazio), sem JSON.stringify

5. Criar `frontend/admin/src/hooks/use-alertas.ts` (per D-04, D-07):
   - `useAlertas(params)`: GET /api/alertas/ com params `{ estado?, nivel?, usina?, page? }` — usa JSON.stringify(params) no useCallback deps (mesmo pattern de useUsinas). SEM polling (alertas nao precisam auto-refresh).
   - `useAlerta(id)`: GET /api/alertas/${id}/ com `id` no useCallback deps. Retorna `{ data: AlertaDetalhe | null, loading, error, refetch }`.
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `recharts`, `react-leaflet`, `leaflet` em dependencies do package.json
    - `@types/leaflet` em devDependencies do package.json
    - `frontend/admin/src/types/analytics.ts` exporta PotenciaResponse, RankingResponse, MapaUsina
    - `frontend/admin/src/types/alertas.ts` exporta EstadoAlerta, NivelAlerta, AlertaResumo, AlertaDetalhe, AlertaPatch, PaginatedAlertas
    - `frontend/admin/src/hooks/use-analytics.ts` exporta useAnalyticsPotencia, useAnalyticsRanking, useAnalyticsMapa com setInterval de 600_000ms e clearInterval no cleanup
    - `frontend/admin/src/hooks/use-alertas.ts` exporta useAlertas e useAlerta
    - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
  <done>Pacotes instalados, 2 arquivos de tipo criados com todos os tipos listados, 2 arquivos de hooks criados com 5 hooks no total, TypeScript compila sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar PotenciaPieChart, RankingTable e DashboardPage</name>
  <files>
    frontend/admin/src/components/dashboard/PotenciaPieChart.tsx,
    frontend/admin/src/components/dashboard/RankingTable.tsx,
    frontend/admin/src/pages/DashboardPage.tsx
  </files>
  <read_first>
    frontend/admin/src/types/analytics.ts,
    frontend/admin/src/hooks/use-analytics.ts,
    frontend/admin/src/components/usinas/UsinasTable.tsx,
    frontend/admin/src/pages/DashboardPage.tsx,
    frontend/admin/src/components/ui/table.tsx,
    frontend/admin/src/components/ui/card.tsx,
    frontend/admin/src/components/ui/badge.tsx
  </read_first>
  <action>
1. Criar `frontend/admin/src/components/dashboard/PotenciaPieChart.tsx` (per D-01, D-08):
   - Props: `{ data: ProvedorPotencia[] }`
   - Importar `PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer` de 'recharts'
   - Constante CORES: `['#8884d8', '#82ca9d', '#ffc658', '#ff7300', '#0088fe']`
   - FILTRAR dados: `data.filter(p => p.media_kw !== null && p.media_kw > 0)` antes de renderizar (provedores sem dados nao aparecem no grafico)
   - Usar `ResponsiveContainer width="100%" height={300}`
   - `Pie` com `dataKey="media_kw"` e `nameKey="provedor"`, `cx="50%"`, `cy="50%"`, `outerRadius={100}`, `label`
   - `Cell` com `fill={CORES[index % CORES.length]}` para cada entry
   - `Tooltip` com formatter: `(value: number) => [value.toFixed(2) + ' kW', 'Potencia media']`
   - `Legend` sem props customizadas
   - Se array filtrado vazio, mostrar `<p className="text-muted-foreground text-center py-8">Sem dados de potencia disponiveis</p>`

2. Criar `frontend/admin/src/components/dashboard/RankingTable.tsx` (per FE-13, FE-15):
   - Props: `{ ranking: ProvedorRanking[], selectedProvedor: string | null, onSelectProvedor: (provedor: string | null) => void }`
   - Tabela shadcn (Table, TableHeader, TableBody, TableRow, TableHead, TableCell)
   - Colunas: # (posicao), Provedor, Inversores Ativos
   - Cada row clicavel: `onClick={() => onSelectProvedor(item.provedor === selectedProvedor ? null : item.provedor)}` — toggle behavior
   - Row selecionada: `className={item.provedor === selectedProvedor ? 'bg-muted' : 'cursor-pointer hover:bg-muted/50'}`
   - Se ranking vazio, mostrar "Nenhum dado de ranking disponivel"

3. Substituir `frontend/admin/src/pages/DashboardPage.tsx` (per D-05):
   - Importar e usar `useAnalyticsPotencia()` e `useAnalyticsRanking()`
   - State: `const [selectedProvedor, setSelectedProvedor] = useState<string | null>(null)` — este state sera passado ao mapa no Plan 02
   - Layout grid (per D-05):
     - Wrapper: `<div className="space-y-6">`
     - Titulo: `<h1 className="text-2xl font-bold">Dashboard</h1>`
     - Linha 1: `<div className="grid grid-cols-1 md:grid-cols-2 gap-6">` contendo:
       - Card com titulo "Potencia Media por Fabricante" e `<PotenciaPieChart data={potenciaData?.por_provedor ?? []} />`
       - Card com titulo "Top 5 Fabricantes" e `<RankingTable ranking={rankingData?.ranking ?? []} selectedProvedor={selectedProvedor} onSelectProvedor={setSelectedProvedor} />`
     - Linha 2: comentario `{/* MapaUsinas sera adicionado no Plan 02 */}` como placeholder
   - Loading state: usar Skeleton do shadcn ou Loader2Icon spinner enquanto loading
   - Error state: mostrar mensagem de erro com possibilidade de retry
   - Exibir `media_geral_kw` do potenciaData em algum lugar visivel (ex: texto acima do grafico ou dentro do Card header): "Potencia media geral: X.XX kW" ou "Sem dados" se null
  </action>
  <verify>
    <automated>cd /home/micael/firmasolar/frontend/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `PotenciaPieChart.tsx` existe em components/dashboard/, importa de 'recharts', usa PieChart+Pie+Cell+Tooltip+Legend+ResponsiveContainer, filtra media_kw null
    - `RankingTable.tsx` existe em components/dashboard/, recebe props selectedProvedor e onSelectProvedor, renderiza tabela shadcn clicavel
    - `DashboardPage.tsx` nao e mais placeholder, importa useAnalyticsPotencia e useAnalyticsRanking, renderiza PotenciaPieChart e RankingTable em grid md:grid-cols-2
    - DashboardPage declara useState selectedProvedor para uso futuro pelo mapa
    - `npx tsc --noEmit` passa sem erros
    - `npm run build` passa sem erros
  </acceptance_criteria>
  <done>DashboardPage renderiza grafico de pizza Recharts com dados da API de potencia por fabricante, tabela de ranking top 5 clicavel, e layout em grid responsivo. State selectedProvedor pronto para integrar com mapa no Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Todas as chamadas passam pelo interceptor axios que injeta Bearer token |
| API response -> UI | Dados da API renderizados no browser — XSS possivel se renderizar HTML cru |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | Hooks de analytics | accept | JWT obrigatorio no backend (ja implementado); interceptor axios injeta Bearer automaticamente |
| T-06-02 | Tampering | Dados do grafico/ranking | accept | Dados sao read-only no frontend; backend calcula agregacoes server-side |
| T-06-03 | Information Disclosure | Tooltip do grafico | accept | Dados de potencia por provedor nao sao sensiveis — admin ja tem acesso total |
| T-06-04 | Denial of Service | Polling a cada 10min | accept | Intervalo de 10min alinhado com ciclo de coleta; sem risco de sobrecarga |
</threat_model>

<verification>
1. `cd frontend/admin && npx tsc --noEmit` — zero erros de tipo
2. `cd frontend/admin && npm run build` — build de producao sem erros
3. Verificar que recharts, react-leaflet, leaflet estao em package.json
4. Verificar que DashboardPage importa e usa hooks de analytics
5. Verificar presenca de setInterval com 600_000 nos hooks de analytics
</verification>

<success_criteria>
- DashboardPage renderiza grafico de pizza com dados reais de potencia por fabricante
- Tabela de ranking mostra top 5 fabricantes com inversores ativos
- Hooks fazem polling automatico a cada 10 minutos (setInterval com cleanup)
- State selectedProvedor existe no DashboardPage, pronto para conectar ao mapa
- Tipos TypeScript completos para analytics e alertas
- TypeScript compila sem erros; build de producao sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-anal-tico-alertas/06-01-SUMMARY.md`
</output>
